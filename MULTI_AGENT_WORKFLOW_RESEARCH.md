# 多 agent 工作流调研附件

调研日期：2026-09-08。

本文服务于 [开发需求](MULTI_AGENT_REQUIREMENTS.md) 中的工作流规划讨论。它区分官方资料描述、本地 dsh 文档事实和面向本系统的建议，不代表已经安装或验证任何框架或通信协议。

产品决定已由用户后续授权汇入主规格：采用可编辑计划、模板与持久任务状态，首版不以 A2A 作为本地协作前提。本文后文的建议与未定选项保留为调研时记录，实施以主规格为准；文档更新不改变以下现有能力和验证限制。

## 取证范围与结果

内置检索子代理最初使用 WebFetch 请求 6 个官方页面，均在 60 秒后超时，未取得正文。随后主会话使用直接 HTTPS 请求成功读取下列 5 个官方页面的正文；未使用搜索摘要代替证据。LangGraph 的 durable-execution 请求返回标题为 Persistence 的页面，因此本报告仅引用实际取得的持久化说明，不据此声称已核实完整的重放或副作用处理机制。未运行任何示例、产品或外部 CLI。本次没有调用真实 Codex 或 Claude Code 进行独立审核。

| 编号 | 已取得正文的官方来源 | 本文引用范围 |
|---|---|---|
| S1 | [A2A Core Concepts](https://a2a-protocol.org/latest/topics/key-concepts/) | Agent Card、Task、Message、Artifact、contextId 及请求响应、流式和推送交互。 |
| S2 | [A2A and MCP](https://a2a-protocol.org/latest/topics/a2a-and-mcp/) | agent 间任务协作与工具、资源接入的区别。 |
| S3 | [OpenAI Agents SDK：Agent orchestration](https://openai.github.io/openai-agents-python/multi_agent/) | 模型决策与代码编排、manager/agents as tools、handoffs、串行、并行及评估迭代。 |
| S4 | [LangGraph：Workflows and agents](https://docs.langchain.com/oss/python/langgraph/workflows-agents) | 预定执行路径与动态 agent、routing、orchestrator-worker、evaluator-optimizer。 |
| S5 | [LangGraph：Persistence（通过 durable-execution 地址读取）](https://docs.langchain.com/oss/python/langgraph/durable-execution) | Checkpointer 与 store 的不同职责、内存存储不能跨进程重启保留状态。 |

A2A 完整规范页与 LangGraph interrupts 页仍只有超时记录，没有取得正文。本文不锁定协议版本，也不将文档示例中的模型或配置值作为本系统选择。官方文档给出可用模式，不能作为某种方式在所有任务中“最先进”或效果最好的证据。

## 概念分层

“loop harness”在本轮未指向某个具体产品，暂按 agent 执行循环或外层迭代运行机制理解，不能据此假定用户已经选择某个循环框架。

| 层次 | 负责的问题 | 依据 |
|---|---|---|
| Harness 与单 agent 执行循环 | 一个 agent 如何接收上下文、请求模型、调用工具、处理结果并继续下一步。 | [dsh turn flow](docs/architecture.md#turn-flow)。 |
| 工作流与编排 | 哪些岗位参与、任务如何拆分、何时并行或交接、依据什么条件返工或结束。 | S3、S4。 |
| A2A 通信 | 独立 agent 服务如何发现对方、提交和跟踪任务、交换消息与产物。 | S1、S2。 |
| MCP 能力接入 | agent 如何调用工具并获取外部资源。 | S2。 |
| 持久状态与记忆 | 如何保存某次运行进度，以及跨任务保存事实或知识。 | S5。 |

这些层可以组合。A2A 规定任务与消息的互操作形式，并不为应用决定岗位分工、业务依赖、预算或验收标准；这些仍是编排策略。此项是基于 S1、S2 所描述职责作出的设计判断，并非已核查完整规范后的排他性协议结论。

## 官方资料列举的协作模式

S3 明确允许混合模型驱动与代码驱动的编排：模型可根据开放任务决定下一步，代码可控制顺序、并行和停止条件。其 manager/agents as tools 模式由管理者保留控制并调用专家处理子任务；handoffs 则将当前轮次的主导权交给目标专家，两者可以组合。

S4 列举串行处理、并行、路由、协调者与执行者、生成与评估迭代等模式。协调者可以按实际任务动态产生子任务，工作数量无需预先固定；评估迭代适合存在明确成功标准、但通常需要多轮改进的任务。框架示例不是独立正确性保证，评估结论仍需与任务要求和证据相符。

对于本系统，以上证据支持将流程设计为可组合的步骤和规则，而不是把每个岗位永远安排在一条固定序列中。具体使用哪种模式应通过任务结果、成本、耗时及可追溯性验证；增加 agent 数量本身不保证提升效果。

## A2A 的能力及边界

S1 中，A2A 服务从调用者角度可以是内部实现不透明的 agent 或 agent 系统，内部工具、记忆和执行方式无需暴露。Agent Card 描述身份、地址、能力、认证要求和 skills；此处 skills 是对外能力描述，不是向调用者分发或安装此前讨论的 SKILL.md 文件。

Task 是有标识和生命周期的工作单元；Message 用于指令、问题、回答及上下文交换，Artifact 表达文档、图像或结构化数据等交付物。contextId 可以关联多个相关任务，但关联标识本身不让两个产品共享原生会话、内部上下文或记忆存储。

S1 描述请求响应和轮询、SSE 流式更新、推送通知三类交互方式；实际使用需要服务端支持相应能力。收到任务或消息不代表任务完成；协议中的完成状态也不等于业务验收已通过。

S2 将 MCP 定位于工具和资源接入，将 A2A 定位于独立 agent 的任务协作，并明确两者可以共同使用。A2A 可用于本系统接入跨进程、跨部署或第三方 agent 服务；它也可以用于内部服务，但本地多 CLI 运行并不因此必须统一改成 A2A。是否采用取决于实际互操作需求，而不是岗位数量。

## 本地 dsh 的现状

以下为本地文档和声明的阅读核实，没有运行行为验证。现有机制可提供开发基础，但不能直接视为完整的用户工作流规划产品。

- [工作流能力](packages/workflow/workflow/README.md)运行模型提供的 JavaScript 脚本，以 agent、parallel、pipeline 等操作组织子任务。其限制明确包含没有运行检查点或恢复、没有保存或嵌套工作流、没有跨子任务 token 预算机制。
- [workflow 工具](packages/workflow/tool-workflow/README.md#durable-session-records)记录部分运行和成员开始结束事件；这些观察记录不同于可继续执行的检查点。不能把“有日志”写成“重启可续跑”。
- [ralph 工具](packages/workflow/tool-ralph/README.md)每轮创建新 agent，仅传递有限交接报告，在共享工作目录中持续执行同一目标；结束依赖执行者自报、阻塞或轮数上限。其限制包含无独立评估、无进程恢复、无每轮动态 provider 切换，不能替代可编辑的通用岗位工作流。
- [子 agent 控制工具](packages/subagent/tool-subagent-control/README.md)支持直接父子之间的消息、终止当前轮次和发现可续接成员；一次性子任务不在可消息交互成员列表中，不能推断已经支持任意成员间自由通信。
- 后续开发路线核查确认另有 [实验性 Agent Teams](packages/experimental/agent-team/README.md)，已经具备成员间持久消息、任务 CAS 和依赖图。它基于 Lead Session 日志、单进程协调及共享工作目录，写入范围只作提示，尚不能等同于本规格的 PostgreSQL 公司状态、多节点协调和强制目录隔离；具体复用结论由实现者核实。
- [ACP 服务](packages/acp/README.md)与 [ACP 子 agent 客户端](packages/subagent/subagent-acp/README.md)使用的是 Agent Client Protocol，不是 A2A。本轮未对整个仓库作 A2A 存在性审计，因此不声称仓库完全没有相关代码。

## 面向需求的组织建议

以下为讨论建议，不是已确认功能清单，也不要求使用 LangGraph、OpenAI Agents SDK 或其他新增依赖。

用户用自然语言说明任务目标、产物和限制，系统形成可编辑的任务计划，以岗位为参与者。任务列表、表单或可视化图可以作为同一份计划的不同编辑视图；具体交互待用户确认。每个步骤明确输入、输出、依赖、执行配置、验收依据和失败处理。

流程可组合串行、并行、条件分支、交接、评估后返工、人工决策、等待事件等操作，循环必须有终止条件和授权范围。流程模板保存可复用的规则；每次执行单独记录任务参数、实际执行配置、产物和进度，避免修改模板影响已经运行的任务而没有记录。

需要探索时，可以允许规划 agent 在授权范围内细分任务和提出计划变更；岗位、模型、权限和审批义务由已授权规则约束。规划建议与实际生效计划分别记录，执行器依据计划和已记录状态安排工作。计划修改适用于哪些未开始步骤、运行中的步骤如何暂停，均需明确定义。

Agent 交接可以保留自然语言说明，同时携带任务标识、交接类型、输入与产物引用、当前状态、证据、问题和期望的下一步。系统负责验证发送者身份与交接权限，按需传递上下文，不自动将所有成员的完整历史广播给所有成员。工作流状态、共享知识和成员私有记忆分别管理。

恢复与重试能力需要明确保存点和外部副作用处理规则。一次超时、断线或消息重发不能直接等同于先前操作未执行；重试前需要确认操作状态或采取去重措施。系统可提供人可查看的步骤状态和运行历史，但可查看记录、重新执行以及安全恢复是不同能力。
