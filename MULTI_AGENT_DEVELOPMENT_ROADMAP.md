# 多智能体软件开发公司：开发路线与工作包

整理日期：2026-09-08。

状态：开发路线已建立，正在按 [节点开发与硬审核规则](NODE_DEVELOPMENT_RULES.md) 启动首个节点。

当前用户安排：真实 Codex 使用 `gpt-6-astra / max` 编写当前节点详细计划；ZCode 按计划实施并修复；真实 OpenCode 使用 `glm-5.3 / max` 硬审核。审核通过前不规划或实施下一节点。原阶段划分及验收范围仍适用，本段替代后文此前尚未安排执行者的状态描述。

目标：按 [v1.0 需求规格](MULTI_AGENT_REQUIREMENTS.md) 分批交付可运行、可验证的软件公司平台，覆盖四种真实 harness、集中铁律与工具、PostgreSQL/GBrain、设计美工及网站/App/微信小程序交付，并最终通过恢复与升级验收。

本文件回答“接下来按需求开发应该怎么做”，规定工作包结果、依赖和验收。它不是逐文件的代码实现方案；详细计划由用户当前指定的真实 Codex 在每个节点开始前编写，ZCode 按计划实施，真实 OpenCode 硬审核。具体模型、顺序与证据要求见 [节点开发规则](NODE_DEVELOPMENT_RULES.md)，不以路线文档替代运行证据。

## 1. 当前起点

本地分支为 master，提交为 `d347e703908d0406b7a7ef80e3a0e594d86b2215`，origin 指向用户的 fork。本轮没有重新检查上游新提交，不把这个提交宣称为此刻最新；正式实施前先比较远端并保留现有需求文档和其他本地工作。

已读到的环境为 Node `v24.18.0`，符合仓库引擎要求；仓库 packageManager 声明为 `pnpm@11.7.0`。当前 PATH 中未找到 pnpm，项目没有 node_modules，因此尚无本次构建和测试通过证据。安装工具和依赖属于启动实施时的基线工作。

需求基线及附件当前均是未跟踪文件。实现者在建立开发分支和变更记录时必须纳入这些文档，不能用清理或同步操作删除它们。是否提交、推送或发布仍按届时的明确授权执行，本轮没有做这些操作。

## 2. 分工与每批交付规则

| 参与者 | 本项目开发职责 | 每批应提供的结果 |
|---|---|---|
| 用户/所有者 | 决定产品方向、具体模型和凭据授权、发布及治理事项；已安排本项目节点执行流程。 | 项目输入与有效授权。 |
| ZCode | 按当前节点计划实施、并行委派独立任务、测试整合和审核返工。 | 实际变更、验证证据和问题闭环。 |
| 真实 Codex CLI | 编写当前节点详细计划和必要修订。 | 使用 `gpt-6-astra / max` 产生的可执行计划和真实调用记录。 |
| 真实 OpenCode CLI | 对照节点计划进行只读硬审核与返工复审。 | 使用 `glm-5.3 / max` 产生的明确结果及证据。 |

实际调用遵守用户届时指定的产品、调用方式、模型与推理设置，以及仍适用的真实身份和独立审核规则。入口不可用时报告具体阻塞，不用替身或内置子代理冒充完成审核。产品需求与开发阶段划分不预先确定具体执行者。

每个工作包拆成有独立行为和验收结果的小变更。一个变更应包含：需求范围、失败表现或待实现能力、实现者设计、代码、配套文档和 Agent Note、适当测试、真实产品验证以及独立审核。不能把所有底层文件写完后才第一次启动产品，也不能把只有空表、空接口或漂亮界面的改动当作业务交付。

运行和检查遵循 [根规则](AGENTS.md)、[包规则](packages/AGENTS.md)、[架构](docs/architecture.md)、[测试政策](docs/testing.md)和[并发及生命周期规则](docs/defensive-patterns.md)。支持的 Node 应用通过 dsh profile 启动，不增加绕开产品入口的临时正式服务。新行为优先利用文档化插件扩展点；具体拆包由实现者根据实际消费者确定。

## 3. 工作包与依赖

| 工作包 | 可审查交付 | 开始前需要 | 主要验收对应 |
|---|---|---|---|
| P0 基线与关键能力验证 | 可重复构建说明、四种 harness 和 PostgreSQL/GBrain 能力证据、现有代码复用结论。 | 当前源码和规格。 | AC-01 至 AC-04、AC-10、AC-16 至 AC-18 的先行验证。 |
| P1 公司控制与受控执行 | 项目、岗位、成员、规则、授权、持久任务、真实执行与版本记录可用。 | P0 已确认可行的入口及事实限制。 | AC-01 至 AC-09、AC-16、AC-21 至 AC-24、AC-31 的基本路径。 |
| P2 首个完整开发任务 | 用户提交小任务，由用户指定的开发者实现、指定的复核者独立审核，产物可验收。 | P1；所需模型/认证配置已获授权。 | AC-05、AC-09、AC-12、AC-20、AC-31 的最小完整场景。 |
| P3 集中能力、记忆与可编辑流程 | 四种 harness 按岗位使用系统资源，GBrain 记忆连续，可编辑并运行多步骤流程。 | P1 的身份、授权、状态和执行记录。 | AC-04、AC-10、AC-17 至 AC-19、AC-29。 |
| P4 软件公司三端交付 | 独立设计美工过程、网站、Android/iOS、微信小程序及交付材料。 | P2/P3；对应构建节点与项目条件。 | AC-11 至 AC-15、AC-20。 |
| P5 平滑升级与恢复验收 | CLI/工具新旧版本并行、兼容回退、迁移拒绝、备份恢复和故障证据。 | P1 的版本/规则/状态基础，P3 的真实服务，P2 的真实长任务。 | AC-21 至 AC-30。 |
| P6 完整首版发布准备 | 汇总三端结果、系统验收、运维说明、已知限制和待发布版本。 | 各工作包适用验收通过。 | AC-01 至 AC-32 全覆盖。 |

P2 不以整个管理界面和流程画布完成为前提，先证明真正能交付一个任务。四种 harness 都是首版接入要求；P0 尽早验证四者，P2 使用用户安排的开发与独立复核组合，不把其余产品推迟到最后才确认可行。

P3 的能力服务与记忆接入可以在 P1 身份约定稳定后并行开发；P4 的设计工具、Android、iOS、小程序验证也可由独立工作包并行。共享状态、身份、规则和执行配置的变更必须由同一责任方协调，不能让并行实现者各自发明不同语义。

## 4. P0：先建立基线并验证关键能力

### P0-A 构建与现状基线

- [ ] 记录源码、依赖锁文件、Node 和 pnpm 版本，确认并保留已有未提交内容。
- [ ] 建立开发分支及隔离的执行配置；若为一次性试验，其文件放在 `D:\Temp_projects` 的语义化子目录。
- [ ] 安装仓库声明的依赖并完成适用构建及最小启动检查；运行结果与环境限制分别记录。
- [ ] 阅读现有相关包与测试，形成“已满足、可复用但需调整、需要新增、能力阻塞”的证据清单。
- [ ] 由实现者给出每个子项目的具体设计、数据责任和测试入口，独立复核后进入对应实现。

结束条件：后续人员能按记录重现项目启动，失败项目有真实输出，未运行项不能写为通过。检查范围按变更决定，不默认每次重复完整覆盖率、全部 e2e 或所有平台矩阵。

### P0-B 四种真实 harness 验证

使用受控测试项目，分别核实产品版本和真实启动、可用模型配置、配置发现、专用数据目录、系统工具连接、取消、续接或交接、用量和原生工具观测范围。全局环境的测试标记必须证明没有进入系统执行，也没有被系统写回覆盖。

必须有允许操作和拒绝操作各一条证据。只证明能输出一句文本不算接入完成；启动失败、取消后仍有自有进程、工具越权或继承全局配置都应形成具体问题交给用户指定的开发者。缺少已授权账号或指定审核模型时不改用替身。

结束条件：得到四种产品的能力矩阵，明确首版强制要求是否可以达到。限制影响某类岗位时，系统不能宣称该岗位已支持；形成范围明确的实现任务或真实阻塞。

### P0-C PostgreSQL/GBrain 联合验证

验证独立平台库和 GBrain 库的账号隔离、固定版本兼容性、pgvector 及 GBrain 启动。使用合成记忆完成写入、跨 harness 读取、更正和失效，验证公司共享、项目和成员 source 的正反向权限。

同时验证并发写入、记忆服务不可用和重新连接时的状态。没有实际数据恢复和权限拒绝证据，不能仅凭建库成功认定数据库与记忆要求已满足。

结束条件：GBrain 接口与平台身份能够对接，失败行为和自动写回差异被明确记录。具体生产凭据和供应商模型不在试验中自行配置。

## 5. P1/P2：先形成能交付的小系统

P1 的首批业务结果是：用户在平台创建项目和任务，配置成员及可用执行配置，系统保存适用规则和授权，启动真实执行，并记录状态与产物。控制台初版只需清楚展示任务、阻塞、授权、运行证据和产物。

PostgreSQL 从这里开始承担平台状态。版本固定、规则版本、操作标识和产物版本也从这里进入记录，不能等到 P5 才补；这些是升级与恢复测试的前提。最早一批任务就验证数据库失联不会被错误确认已接单，以及恢复后不会重复派发已确认动作。

P2 选择参考网站中的一个小业务功能作为完整场景，包含明确需求、有限项目目录、实际实现、测试、用户安排的独立复核和可查看的产物。优先在隔离预览环境验证，不将首个开发试验直接发布到生产。

结束条件：同一份任务可以追溯输入、授权、实际开发和审核执行者、测试结果、产物版本及验收状态。若执行者自报完成但没有产物或审核失败，系统正确保持未验收，不以状态按钮手工盖过证据。

此时系统还不是完整软件公司：GBrain 连续性、完整流程编辑、设计美工和三端构建由后续工作包完成。

## 6. P3：集中能力、记忆和流程规划

工具服务和 skill 提供必须与 P1 的成员、岗位、项目、授权和版本关联。使用系统登记能力，验证不同 harness 所见集合与实际可调用集合一致；原生 CLI 仍可执行的 Shell/文件路径要纳入权限验证，不能只检查 MCP 菜单。

GBrain 通过平台稳定身份访问已授权 source，验证新会话和 harness 切换后记忆仍可访问，用户纠正后的结论能被新任务取得，成员私有区域仍不可越权。平台的任务和铁律状态不搬入语义检索作为唯一控制来源。

流程编辑在同一任务模型之上提供自然语言、结构化表单与流程图，支持依赖、并行、条件、返工、审核和人工决定。保存模板和运行版本，确认修改模板不偷偷改变活跃实例。

结束条件：四种 harness 能执行同一类获准的岗位任务、使用中央资源和记忆；简单多步骤流程具备可查看、可变更和可恢复的明确状态。每个成员消息的接收、执行结果与验收状态分别记录。

## 7. P4：完成软件公司交付能力

产品、UX/UI 和美工岗位先交付参考业务的流程、交互原型、组件规范与素材；测试岗位据此形成验收用例。设计源文件、素材许可和真实渲染证据关联任务和版本，不能只记录一段设计描述。

工程工作拆为网站/后台、后端接口、Android/iOS App、小程序及构建发布准备。公共业务接口和共享设计规范由责任岗位维护，各端独立验证自己的交互、错误状态、构建与运行行为。

iOS 构建节点、签名和小程序 AppID 等需要的真实条件提前在 P0 能力矩阵登记。缺少条件时可以完成不依赖它的设计与源码，但必须明确哪一项最终验收未完成，不能用 Web 预览替代移动平台通过。

结束条件：参考产品三端的源码、设计、美术资源、接口、构建/预览、测试与独立审核以及部署说明齐全；正式商店和生产发布按已有或单次具体授权执行，不因开发任务完成自动发布。

## 8. P5：平滑升级、失败恢复和长期运行

P5 集中验证运行连续性，但相关版本和数据基础必须在 P1 即设计并实现。首先用受控组件版本模拟升级与失败，再对已获授权的真实兼容 CLI/工具版本验证；不能直接在唯一工作环境试升级。

核心验收场景是：启动固定旧版本的真实流程，部署并验证新版，新流程使用新版，旧流程及其后续步骤仍使用原版本；新版本故障时只回退新任务路由，不杀死仍在工作的旧实例，不重复已经完成的外部操作。

对 GBrain、PostgreSQL、任务状态和 dsh Session 记录分别核对数据兼容性。破坏性迁移无法在线兼容时明确维护窗口和恢复影响，不靠简单切回程序目录声称已回滚全部数据。

运行故障场景覆盖 worker 崩溃、取消、网络中断、结果回传丢失、数据库失联、限流、预算耗尽和重复消息。数据恢复必须在独立测试环境实际完成并检查权限、引用和任务进度。

结束条件：AC-21 至 AC-30 有真实故障或升级证据；未通过的恢复路径标明限制，不将“程序自动重启”当作业务恢复。

## 9. P6：首版验收和发布准备

逐项核对主规格的 AC-01 至 AC-32，保留测试环境、版本、证据、失败记录和责任人。依据三端代表性任务记录交付成功率、耗时、费用、人工介入和不可观测项，不用框架宣传或模拟测试替代系统效果。

完成安装启动说明、专用数据位置、账号配置方式、铁律管理、升级回退、备份恢复、已知限制及故障处理说明。候选发布版本绑定需求、设计、测试和指定独立审核记录。

最终交付必须区分可运行源码、预览通过、构建通过、签名/平台验证和真正已发布状态。外部发布授权是具体产物和目标的授权，不由本开发路线自动授予。

## 10. 实现者需要优先阅读的本地位置

以下是核实复用与具体设计的入口，不是本路线要求直接修改的文件列表。

| 主题 | 已存在的入口 | 核实重点 |
|---|---|---|
| 启动与配置 | [架构](docs/architecture.md)、[app-boot](packages/boot/app-boot/README.md) | 新平台能力遵循 profile/bundle 组合，不另起绕过 dsh 的正式 Node 启动入口。 |
| 真实 CLI 委派 | [Codex](packages/subagent/subagent-codex/README.md)、[Claude Code](packages/subagent/subagent-claude-code/README.md)、[ACP](packages/subagent/subagent-acp/README.md) | 一次性调用、原生配置发现和权限/续接限制。 |
| 团队协作 | [Agent Teams 类型](docs/subsystems/agent-team.md)、[实验实现](packages/experimental/agent-team/README.md) | 已有 roster、任务 CAS、持久消息；共享目录、单进程和 advisory 写范围不等于本规格。 |
| 工具权限 | [工具服务](packages/core/tools/README.md)、[MCP 客户端](packages/mcp/mcp-client/README.md) | 模型所见权限与实际执行权限，对外提供系统能力与连接外部 MCP 的不同方向。 |
| Skills | [技能能力](packages/skill/README.md) | 内容来源、目录读取、全局发现路径及固定版本。 |
| 流程 | [workflow](packages/workflow/workflow/README.md)、[ralph](packages/workflow/tool-ralph/README.md) | 脚本编排与循环不直接提供本规格的 PostgreSQL 检查点、模板或升级。 |
| 会话记录 | [session log](docs/architecture.md#session-log) | 模型可见记录和已发布格式的相邻迁移，不覆盖旧记录。 |
| 测试与生命周期 | [测试政策](docs/testing.md)、[防御性规则](docs/defensive-patterns.md) | 真实入口、并发资源、取消后确认停止、快照和实际世界验证。 |

Agent Teams 需要优先评估复用：它已存在可持久恢复的团队消息与任务记录，不能继续笼统表述为 dsh 完全没有相关能力。但其当前状态以 Lead Session 为来源，成员共享工作目录，协调为单进程，writeScopes 只作提示；不能直接照搬成 PostgreSQL 公司控制层或多节点权限系统。

## 11. 检查与证据要求

代码级设计应为每项行为选择真实的最高适用测试入口：权限验证必须直接尝试被禁止操作，取消必须确认自有进程实际停止，持久状态必须跨重启检查，模型或用户可见变化必须有对应记录/快照。

遵循仓库要求运行与变更相关的单元、真实组合、构建产物、无 Key 会话快照及必要的真实 API/产品测试。缺少 Key 时跳过结果必须明确记录，不能把该项写为真实模型已通过。SessionEventMap 或生命周期改动同步验证 TypeScript 与 Python SDK 投影。

非平凡实现包含 Agent Note、README/JSDoc、适用双语文档及生成记录。具体检查命令由实现者根据所改包和仓库脚本核实；CI 负责完整覆盖率与平台矩阵，不为每次小变更重复跑全仓全部测试。

每次审核交付包含真实变更版本、运行过的命令与结果、未执行项及原因、已通过 AC 范围、剩余阻塞和回退影响。外部 CLI 模型设置、软件供应商或数据迁移的变更不得被“测试需要”自动授权。

## 12. 完整覆盖与排期原则

| 工作包 | 主规格需求覆盖 |
|---|---|
| P0 | REQ-001、REQ-002、REQ-003、REQ-004、REQ-007、REQ-016、REQ-022 的现状与可行性。 |
| P1 | REQ-002、REQ-003、REQ-004、REQ-005、REQ-010、REQ-011、REQ-012、REQ-013、REQ-016、REQ-017、REQ-018、REQ-019、REQ-020 的版本基础、REQ-021。 |
| P2 | REQ-001、REQ-008 的首个场景、REQ-012、REQ-013、REQ-014、REQ-017、REQ-019。 |
| P3 | REQ-006、REQ-007、REQ-015、REQ-016、REQ-018、REQ-019。 |
| P4 | REQ-005、REQ-008、REQ-009、REQ-010、REQ-014、REQ-022。 |
| P5 | REQ-003、REQ-011、REQ-017、REQ-018、REQ-020、REQ-021、REQ-022、REQ-023。 |
| P6 | REQ-001 至 REQ-023 的完整交付与适用 AC 全部核对。 |

不在构建未启动、四种真实入口未验证前给出保证工期。完成 P0 后，按真实缺口、节点和模型可用性形成工作量估计；完成 P2 后再校准剩余交付速度。设计美工、平台集成与多端构建可并行，但不以多个 agent 同时改相同底层文件来压缩排期。

当前第一个实施范围属于 P0，真实 Codex 正在据实际源码编写首个独立节点计划。基线、四种 harness、PostgreSQL/GBrain 和 Agent Teams 的完整 P0 验证仍须逐节点通过，不能把模型探针成功等同于产品适配完成。

## 13. 计划粒度与当前完成状态

需求规格回答“做成什么”；本路线回答“分几阶段、顺序和验收是什么”；节点实施计划才回答“改哪些模块和文件、接口与数据如何设计、步骤怎样执行和验证”。阶段路线已完成，当前正由指定真实 Codex 编写首个 P0 节点计划，其余节点在前节点硬审核通过后才规划。计划和通过状态以节点目录中的原始计划、候选和审核回执为准。

| 阶段 | 已完成的阶段规划 | 开工前需补充的详细计划 |
|---|---|---|
| P0 | 已分为构建基线、harness 验证、PostgreSQL/GBrain 验证三组，有验证目标。 | 实际环境、安装/测试版本、命令、隔离样本、验证用例、证据保存和清理方式。 |
| P1 | 已有基础模块范围、依赖及验收目标。 | 数据实体和表、接口、权限规则、任务状态、版本基础、文件改动及迁移测试。 |
| P2 | 已定义最小完整开发交付场景。 | 具体功能、输入样本、步骤、产物、测试和独立审核验收操作。 |
| P3 | 已定义中央工具、记忆、流程规划的范围。 | 对外接口、成员/source 映射、兼容处理、模板数据、交互及权限测试。 |
| P4 | 已定义设计美工、网站、App、小程序交付范围。 | 可分别执行的设计、各端工程及构建子计划，真实节点和平台验证步骤。 |
| P5 | 已定义升级、故障和备份恢复场景。 | 新旧版本方案、兼容矩阵、迁移操作、故障注入、恢复与回退测试步骤。 |
| P6 | 已定义全量验收和交付范围。 | 按实际交付版本编制验收执行表、证据索引、发布操作及复核材料。 |

详细计划按阶段滚动细化：当前阶段开工前达到可执行粒度，后续阶段保留稳定目标和依赖，待前置阶段产生真实接口与验证结果后再细化。跨阶段基础如成员身份、数据归属、权限、任务状态和版本规则需要早期统一，不等到后期重新设计。

每份详细计划至少包括：范围与关联需求、前置结果和依赖、技术设计与必要接口/数据定义、创建或修改的文件、可以独立验证的任务、准确测试入口与预期结果、迁移/清理/回退要求、完成标准和证据。复杂阶段拆成数份子计划，常规小任务不重复撰写整套计划。

详细计划和执行分工按 [节点规则](NODE_DEVELOPMENT_RULES.md) 进行。只有当前节点真实 OpenCode 硬审核通过，才进入下一节点；本节不以阶段路线冒充已完成的详细计划。
