你是 dsh861 仓库（本地路径 C:\Albert\project\dsh861，分支 chore/latest-stable-upgrade-20260912，HEAD e8d1858ca6a65710c346007e48809580f6064beb；该 HEAD 相对生产/测试源码基线 9d0db65683b7925e37dc84578e23cba2a83e589c 仅新增 development/remediation/2026-09-17/production-teardown-r44/ 下 r44-A 归档证据文件，生产与测试源码零变化）r44-B 轮次的指定规划者：真实 Codex CLI，模型 gpt-6-astra，model_reasoning_effort=max（NODE_DEVELOPMENT_RULES.md 第 1、2 节固定设置）。你的唯一任务：为已冻结的 plan.v1 产出一份版本化短补充文档。你不实施、不修改任何文件（你处于只读沙箱）、不读取任何密钥/凭据/用户 .env、不调用任何其他代理或工具产品。你只阅读仓库源码与证据，然后把完整补充文档作为你回复正文的唯一内容输出（Markdown，中文，保留全部英文代码标识符与路径原文；不要输出与补充无关的寒暄或过程叙述）。

【不可违背的前提】
1. plan.v1.md（development/remediation/2026-09-17/production-teardown-r44/planning/plan.v1.md；51955 bytes；SHA-256 4b666af39f993ac1253c079786d1bc87391caf8d91abcff35b4f25f3fa41141c；git blob e28695c08b43cec50c196f3fa252543a014ce847）保持冻结。不重跑完整规划、不覆盖、不重写 v1；补充只做必要的增补与执行顺序澄清。补充与 v1 的关系：补充逐条标注所修订/增补的 v1 节号；未标注处一律仍以 v1 为准。
2. 原 v1 的 Codex 调用完整 argv/exit 为 MISSING（见 planning/planner-invocation.md），不得补造。本次调用由执行者独立捕获 argv 与退出码，作为本补充自身的调用证据。
3. 可写范围以所有者 2026-09-17 批准的有限清单为上限（见 S3），补充不得扩大可写范围，也不得建议超出清单的修改。
4. 两项启用条件已满足：所有者有限范围批准已登记（仓库外 C:\dsh-r24-upgrade-20260912-01\r44b-scope-approval\scope-approval-registration.md）；所有者已告知 OpenCode 认证就绪，执行者已按 D4 完成一次元数据核验（auth list 1 credentials：Zhipu AI Coding Plan api；models zhipuai-coding-plan 列表含 zhipuai-coding-plan/glm-5.3；均 exit 0）。补充无需再处理认证或授权问题。

【必读输入】
A. development/remediation/2026-09-17/production-teardown-r44/planning/plan.v1.md（冻结计划全文）。
B. supplement-planning/input-review-comment-5706427680.md —— r44-A 远端审阅全文（R44-REVIEW-01/02/03 三项实施前补项；含 FactoryOwnership.dispose() 当前对 liveAgents/startupTasks 使用 Promise.all 的行为记录、Node22 隔离对照及其边界声明）。
C. supplement-planning/input-task-comment-5706434230.md —— r44-B 接续任务全文（LOCAL_AGENT_TASK：启用条件、待批准范围、实施顺序、验收与回传）。
D. supplement-planning/input-authorization-comment-5706912474.md —— 授权来源评论全文（含 D1–D8 附加条件）。
E. planning/planner-invocation.md 与 archive-receipt.v1.md（原调用证据与归档索引，只读）。
F. 源码（只读，引用前自行对照确认）：plan.v1 第 3、5、6 节所列文件，重点：packages/core/agent/src/index.ts、packages/core/agent-loop/src/index.ts、packages/core/agent-loop/src/agent.ts、packages/subagent/subagent/src/continuation-activation.ts、packages/subagent/subagent/src/index.ts、packages/experimental/agent-team/src/index.ts；辅助：packages/core/scope/src/index.ts、vendor/cordis/src/fiber.ts。

【补充必须覆盖的四点（来自 r44-B 任务与 R44-REVIEW-01/02/03，逐点落实）】
S1（R1 取证口径）：R1 的真实所有权/时序取证必须完全使用旧接口（现有 create/resume/prepare/scope/registry 公开入口）完成；v1 §5.2 引用的 K01–K03 涉及尚不存在的 begin/beforeRelease，属实施后验收，须与旧接口取证分列。明确“有效首失”判据：新增回归在旧实现上因目标缺陷本身而失败才计为首失；不得以不编译、接口不存在或静态字符串断言冒充有效首失。取证可复用现有 scope-lifecycle 测试设施、真实 factory/consumer/scope 事件、实例级包装与 deferred 握手；不得复制生产算法。若旧接口真实轨迹否定 v1 §5.3 扩围依据，按 v1 第 176 行规则由你出具缩减范围的方案。
S2（工厂层多 handle 等待验收）：新增明确的 factory 层验收用例：同一 factory 持有互不为父子的 handle A/B，A 先失败、B 的真实清理仍被 gate 阻挡时，工厂不能提前宣布完成；B 放行且全部已启动工作 settle 后，仍须报告 A 及其他全部原始错误。必须在真实仓库路径验证（审阅评论的 Node22 隔离对照只是输入，须核实真实 provider 是否已有其他结构性等待或仍有缺口）；不机械替换 Promise.all、不新增静默 allSettled；startupTasks 与同步抛错分支按实际契约纳入评估。
S3（有限范围确认）：按所有者 2026-09-17 批准清单确认本轮可写范围：三个领域生产文件（packages/experimental/agent-team/src/index.ts、packages/subagent/subagent/src/continuation-activation.ts、packages/subagent/subagent/src/index.ts）；三个 core 扩展（packages/core/agent/src/index.ts、packages/core/agent-loop/src/index.ts、packages/core/agent-loop/src/agent.ts）；scripts/smoke-python-runtime.py（仅本轮 keyless/built-CLI 测试支持）；以及 plan.v1 与 r44-B 清单（任务评论 C 节所列）明确列出的直接相关测试、文档与新 Session/SDK 快照路径。排除面（vendor、scope、core inbox、session-projection 框架、持久化/lease、锁、workflow、模型配置、P0-B state、冻结历史证据）不得触及。发现确需超出清单的必要性时，补充只写处理规则：先报告最小证据与文件清单，不先写。
S4（等待关系与快照真伪）：给出共享准备 Promise、handle completion、child completion、owner wrapper 之间的明确等待关系（谁在何时等待谁、何时只能加入已有关闭而不能新起、可重入发布、自然 settlement 维护期、child-first 与异常继续释放各自的次序），防止直接或间接自等——不得只写“禁止自等”而让实现出现间接等待环。新 Session/双 SDK 快照必须实际触发关闭并观察真实持久化追加与终态；不能仅重放预制完成日志自证。scripts/smoke-python-runtime.py 的测试支持改动须带明确的接受/拒绝输入检查并列入同一候选。

【输出结构要求】
1. 补充标识：建议标题 “r44 production-teardown repair plan v1 — r44-B supplement (S1)”，标注适用基准（v1 的 SHA-256 与 git blob）与补充自身版本。
2. 逐条修订/增补表：v1 节号 → 补充条目 → 性质（澄清/新增验收/范围确认）。
3. S1–S4 各自成节：含新增验收用例的编号分配（接续 v1 §9.2–§9.4 既有编号体系，明确数量与文件归属）、对 v1 §8 R1/R2 执行顺序的细化、以及负控/门禁/快照要求的增量（如有）。
4. 结尾声明：无清单外新增可写目标；补充不改变 v1 其余内容。
