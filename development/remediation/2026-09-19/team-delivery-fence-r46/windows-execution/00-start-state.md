# r46 start state (verified before any edit)

- Local UTC date: 2026-09-19. Workspace `C:\Albert\project\dsh861`, repository `wmqfl861/dsh861`, branch `chore/latest-stable-upgrade-20260912`.
- `git rev-parse HEAD` = `3c9f40f8211894631a65764a18801f0ac77e2ba6`; `git ls-remote origin refs/heads/chore/latest-stable-upgrade-20260912` = same SHA (no remote drift; no fetch performed).
- Working tree before edits: only the three declared r44 preserved artifacts untracked (`development/remediation/2026-09-17/production-teardown-r44/planning/cli-availability-probe.md`, `review/BLOCKED-opencode-hard-review.md`, `windows-execution/01-first-failure.normalized.log`); kept, not uploaded, not cleaned.
- Fixed start blobs verified against the r46 task comment: `snapshots/sdk/agent-team-teardown/teardown-trigger.mjs` git blob `c2e3c48601463a897871da2e0c80a3408f289dd7` (7807 bytes), `snapshots/sdk/teardown.snapshot.ts` blob `6b78bda5931bbb72f36e20258501da6446838679` (25387 bytes), read-only production source `packages/experimental/agent-team/src/mailbox.ts` blob `6afc7f441bcaed56d7706c4fe1fbf39ab9f8192c` (14302 bytes).
- Authoritative task comments read in full via credential-less `api.github.com` REST (HTTP 200 both): issue comments `5737736204` (A-H tasks) and `5737727343` (forensics and controlled comparison). Fetched comment JSON kept outside the repository (`C:\dsh-r46-tmp\`).
- Toolchain: Node v26.8.2 + pnpm 12.4.1 from the dual-directory prefix `C:\dsh-r24-upgrade-20260912-01\node-v26.8.2-win-x64` + `C:\dsh-r24-upgrade-20260912-01\pnpm-12.4.1` (CI ran Node 26.9.0/pnpm 12.4.1; local difference recorded, not upgraded). System pnpm `.cmd` shims and the jscpd `.cmd` shim are broken (known from r44/r45): every pnpm-bearing command in this round ran with the dual-directory prefix, and jscpd ran as `node node_modules/jscpd/run-jscpd.js`.
- r45 finding A honored: every Vitest invocation used the direct entry `node node_modules/vitest/vitest.mjs …` with the same configs (`--project thread-safe` under the root config; `--config vitest.snapshot.config.ts` for the snapshot lane), never the `pnpm exec vitest` wrapper. `TMP`/`TEMP` redirected to the spaceless ASCII `C:\dsh-r46-tmp` inside the run commands. The canonical `pnpm exec` forms from the task are therefore NOT claimed as passed; the direct-entry forms with identical config and targets are recorded instead.
- Raw run logs live outside the repository under `C:\dsh-r46-tmp\raw\` (one file per physical run, exit codes appended); normalized copies plus a dual-hash manifest are committed under `logs/`. No user profile, real HOME, credentials, keys, or `.env` were read; no real model API or E2B was invoked.
