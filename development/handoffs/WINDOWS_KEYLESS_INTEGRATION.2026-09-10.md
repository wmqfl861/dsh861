# Windows 验证、升级收尾与 P0-B 交接

仅处理 `wmqfl861/dsh861`，本地为 `C:\Albert\project\dsh861`。继续 `chore/latest-stable-upgrade-20260912` 和草稿 PR #13，接收基点 `91715660dbbaf743becd0ccdcbb9b2df82a9bf12`。模型配置两文件、节点状态、补丁／依赖锁及历史回执本轮均不修改；P0-B 仍 blocked，不合并 master、不开放 P0-C，不代写指定计划或硬审核。

## 已接收的升级与准确剩余项

[r25 执行回执](../remediation/2026-09-13/upgrade-consumers-r25/verification.json)记录延期主版本迁移、Cordis 三方重放、真实编译和浏览器验证，以及最终补丁哈希整改。保留这些成果，不重新安装工具链、扫描新版本或重做同一轮升级。当前源码与依赖保持已固定批次。

回执中两个失败集合不同：client 5269/5270 的一项是 `present-open.host` 符号链接权限问题；目录选择器的 Loader 组合另为 15/16，失败断言是界面载入失败后后端未被移除。不要把其中一个归并、忽略或报告为另一个。Loader 的 Vitest 池 CSPRNG 崩溃与直连通过同样分别保留，现有观察不是整个平台或所有配置均无缺陷的证明。

## r26：失败启动不能被相同依赖通知重新放行

本轮仅改动一个运行时文件：[vendor/cordis/src/fiber.ts](../../vendor/cordis/src/fiber.ts)。启动失败会主动把执行 epoch 改为 INACTIVE，但这不代表依赖真的不可用；原恢复条件因此允许相同依赖的通知清除失败并重启，清理期间也可排入下一次启动。现独立保存失败依赖 epoch，相同依赖保持失败；真正丢失后恢复、更换提供者、显式更新仍可重试，已销毁实例不复活。

[本轮记录](../remediation/2026-09-13/fiber-recovery-r26/verification.json)保存精确基线源码的局部复现及修复后结果：8项控制在原代码5过3败，在修复版8过0败。使用真实 Fiber 全部源码，但反射、事件和堆栈拼接为显式替身；不是完整 Context／Loader／Windows 实跑，也不是严格类型检查。新增[8项正式测试](../../packages/host/directory-picker-auto/tests/failure-recovery.spec.ts)使用实际 Context 和依赖通知，尚待本地执行。

这项核心错误已局部复现，但尚未证明只改它就能闭合目录选择器的完整组合失败。原目录选择器测试及其断言一字未改。必须通过该实际入口验证；仍失败时沿同一调用链最小修复，不能删除断言、先不创建后端、放宽超时或反复扫除泄漏来换取通过。

## 本地执行：一次聚焦验证，不重做升级

安全同步后沿用已安装 Node 26.8.2、pnpm 12.4.1、Vitest 5 和当前依赖，不自动 stash、重置或强推。首先运行正常仓库配置中的新增恢复测试与目录选择器两套现有测试，保存原始输出、退出码和源码身份：

```sh
pnpm exec vitest run packages/host/directory-picker-auto/tests
```

随后复验本次共享生命周期影响的 app-boot、webserver 以及既有 Fiber 生命周期控制，不重新执行无关的工具包、TLS、CLI版本、完整升级或浏览器全部102文件。现有新工具链下的正常依赖准备命令可复用；不要用本轮局部作者 harness 代替正式8项。目录选择器成功时核实失败载入仍拒绝、后端及服务消失、后续通知不复活；真实服务恢复、显式更新和正常销毁仍须通过。

按实际影响执行类型／构建、lint及文档检查。[Vendor 修改日志](../../vendor/README.md)已补条目；新 Agent Note 核对双语后只点名重录，不用 `--write --all`。锁和补丁未变时复用91715660的干净树 frozen 证据并核对blob；只有确需更改依赖时再用包管理器重建锁，不剥补丁空白、不手编哈希。

## 平台事项和终验日志

不要重复已收集的7行 CSPRNG 矩阵或删掉 `SystemRoot`、关闭加密／沙箱来求绿。可复用直连 runLoaderSmoke 的真实结果，但保留 Vitest 池路径未通过；本轮核心修复与该平台问题无等价关系。符号链接权限项也不得通过 UAC、ACL、安全设置变更来处理，未获权限时保留准确限制；能在已有合法环境完成同一语义测试才记对应平台通过。

这一轮实际执行的最终日志必须随脱敏回执归档、可从远端取回，包含文件成员摘要、命令、退出码及对应候选。不要凭 verification.json 重造上一轮未保留的原始日志，也不机械重跑所有旧验证只为凑归档。明确区分本轮独立复验、上轮报告及缺失的原始证据。

## 交付

同一分支正常提交推送，PR #13 保持草稿；不要再生成相同升级申请。返回最终SHA、新8项和原目录选择器／生命周期回归结果、实际修复差异、必要门禁、可取回最终日志，以及仍存在的两个平台限制。PR说明由远端维护，无需安装gh。

仍不读取生产Key或全局认证，不登录、不请求模型，不动共享账号／ACL／Firewall／WFP／UAC，不使用Remote Desktop Commander、不操作其他项目。三个不可达vendor来源与真实模型／系统授权继续单列，不把这些限制扩大成每轮重复安装、重新配置或新的通用前置框架。
