# Windows 验证、依赖升级与 P0-B 交接

仅处理 `wmqfl861/dsh861`，本地项目为 `C:\Albert\project\dsh861`。PR #12 已接收 `3d6a3ba85c0129afe2dedd421aea55e0914c103b` 的 r23 文件验证，合并提交为 `5434305c5dcf7ddc3ebf939226647b7b608335e6`。旧工具包保持未激活，原回执不改写。mtime 不能独立证明未执行；接收依据是已提交的命令范围和结构化报告，远端未重跑 Windows 或解包本机归档。P0-B 仍 blocked，不合并 master，不生成指定计划或硬审核。

## r24：用户已明确授权项目整体升级

本轮授权覆盖四个 agent、项目依赖、内嵌项目和构建工具的版本升级、实际锁文件重建及必要兼容性迁移，取代旧交接中“Node/pnpm/pnpm-lock 不得更改”的本轮限制。只限定本项目及专用工具目录，不覆盖全局安装、用户其他项目或系统沙箱初始化。模型名称、provider、endpoint、思考等级、凭据引用及模型锁保持原字节；不得因新程序不接受配置而悄悄更换模型或重算模型锁。

任务分支为 `chore/latest-stable-upgrade-20260912`。本轮[升级范围及已查目标](../maintenance/latest-upgrade.r24.json)明确区分版本声明、已安装版本和未完成迁移。当前提交只完成首批声明和工具包适配，`pnpm-lock.yaml` 尚未重建，不能视为可部署或所有项目均已升级。不要直接用 frozen 安装报错再次要求用户授权修改锁文件；在升级分支按原生包管理器生成真实锁，然后才执行 frozen 重装验证。

## 首批已提交变化

Codex 声明改为 0.154.0；Claude Agent SDK 为 0.3.269、Anthropic SDK 为 0.125.0。根开发工具声明 Claude Code 2.1.269、OpenCode `opencode-ai` 1.18.30、指定 Grok 包 `@xai-official/grok` 1.0.25，供本项目专用调用，不代表四套产品适配已验收。根包管理器改为 pnpm 12.4.1；Node 26.8.2 和 TypeScript 7.0.2 仍是待本地安装／迁移的目标。执行批次开始时从官方发布和实时 registry 再核对一次；有更新则固定新版本及来源，不在每次启动时追随浮动 latest，也不因更旧的 latest 标签而降级当前预览系列。

[沙箱工具准备器](../../scripts/p0-b/windows-credentials/prepare-sandbox-tool-bundle.mjs)默认使用[0.154.0 三文件清单](../../scripts/p0-b/windows-credentials/sandbox-tool-bundle.0.154.0.json)，可用显式 `--version` 选择已有的固定历史清单。未知版本、路径穿越、预发布或清单版本不匹配都拒绝；没有任意清单路径和缺失回退。0.149.1 清单和旧回执保留，不能用于认证新二进制。新清单主程序摘要来自官方原始 exe，必须实际核对 npm 安装的主程序字节，不能仅凭版本号认定同一个二进制。

## 本地应连续完成的升级工作

先安全同步、保留现有本地工作。枚举所有跟踪的包清单、vendor、native/Python 锁、CI 引用和部署镜像，逐项记录原版本、已查询最新目标、实际结果及必要证据。范围是所有实际使用的项目，不是只跑四个版本命令。未接入的 GBrain 或数据库组件按实际发现标为尚未集成，不能编造安装版本；不迁移真实数据库。

先检查并合并更新的 dsh 上游源码，再处理其最终依赖图，避免两次大规模重建。已查官方 master 为 `c291e7961a515f6d7af9304e7fd1d257929aef26`；它是源码快照，不是稳定 release 声明。按历史与文件差异进行 merge-forward，保留本项目需求、模型保护、运行安全修复和原始证据。vendor 按已有清单追踪正确 fork，重放本地修改及 rescope，不把本地工作区包替换成同名 registry 包。不存在安全自动迁移时，保留独立阻塞，不强推、整块选择 ours/theirs 或直接覆盖整个仓库。

Node/pnpm 使用新专用工具目录，不卸载或覆盖全局版本。依赖升级先实际解析再安装；单一责任者维护 pnpm 锁，其他独立清点可并行。直接依赖、optional、peer、override、patch、catalog、构建工具与其传递闭包均要覆盖；优先升级拥有依赖的上游包，不强行把不兼容的所有传递依赖 override 到最高主版本。最低发布年龄、安装脚本、证书和钩子不能整体关闭，新增例外要精确到经核验的包版本。旧补丁必须迁移或以新源码证明已不需要，不能直接删除来让安装通过。

TypeScript 7 不能当作旧编译器 API 的原位替换。参照[官方迁移说明](https://devblogs.microsoft.com/typescript/announcing-typescript-7-0/)使用最新原生编译器，同时为仍依赖编译器 API 的脚本解析最新的官方 TypeScript 6 兼容包，明确两者用途。核对包别名、peer 与 `tsc`/`tsc6` 的实际解析，替换旧的 `node node_modules/typescript/bin/tsc` 等构建入口，不能通过放宽 peer、跳过类型或仅 transpile 宣称成功。此兼容安排须真实安装和编译后报告，不把保留旧编译器偷偷写成已用最新版。

新 Node、CLI、SDK、平台包、辅助程序路径和摘要改变后，更新现行消费路径／测试中的版本绑定，复验受影响的真实入口、协议、取消、输出与模型配置不漂移；历史文件不批量替换。可在隔离无凭据环境运行新版的版本／帮助或本地合成协议测试，不执行会初始化共享账号/ACL/防火墙的命令。0.149.1 的原生能力结论不能自动覆盖 0.154.0，需要按新源码与实际无副作用条件重新判断。

## 检查、证据与交付

新准备器测试为原 23 项加新增 12 项，共 35 项；实际次数以执行结果为准。完成更新后的 frozen 安装、必要 host/client 构建和类型、lint、受影响行为与 Loader、文档门禁。新增脚本只通过语法检查不能冒充被 lint 覆盖。运行工具链改变时旧结果并非当前候选证明，但也不需要逐轮机械重跑所有无关平台矩阵。失败先保存，修复后复测受影响项。

[当前工具包文档](../../scripts/p0-b/windows-credentials/sandbox-tool-bundle.md)和拥有它的 Agent Note 双语已同步。只对实际语料中的 Agent Note 点名重录侧车；scripts 非 README 参考不要伪造侧车。更新上游和依赖后产生的生成目录、配置目录、许可证记录和文档变化按实际生成器处理，不修改历史回执。

在同一升级分支按组件组正常提交、推送，保持草稿，所有层次明确已更新／已是最新／不兼容待修／未使用／未能核实。不能只交版本清单就停止，也不能把查到版本当已安装。确实遇到需系统权限、凭据或大规模业务决定的项时，先完成其他独立工作再汇总具体阻塞；不得重新索取已经给出的升级许可。不创建重复安装框架或重复启动申请。

本轮仍不用真实 Key，不读全局认证、不调用收费模型、不触发登录、不初始化沙箱、不复制旧账号秘密，不删除已有全局 Codex 或沙箱对象。依赖升级与模型授权、系统部署、指定 Codex/OpenCode 正式规划／审核彼此独立；网页后台和多团队需求不变。
