# Windows 验证、升级收尾与 P0-B 交接

仅处理 `wmqfl861/dsh861`，本地项目 `C:\Albert\project\dsh861`，继续 `chore/latest-stable-upgrade-20260912` 和草稿 PR #13，接收基点 `91715660dbbaf743becd0ccdcbb9b2df82a9bf12`。本轮并行收到两个 r26 定向修复候选：附件补丁 `dsh861_r26_candidate.zip`（Loader 事务性，作者会话无写接口）与远端提交 `4d6c7512`（Fiber 失败闩锁，已推送）。两者改动文件不重叠（仅本交接文档与 vendor 登记交汇），按逐项审阅整合在同一分支，不回退远端。模型配置两文件、节点状态、补丁／依赖锁及历史回执本轮均不修改；P0-B 仍 blocked，不合并 master、不开放 P0-C，不代写指定计划或硬审核。

## 已接收的升级与准确剩余项

[r25 执行回执](../remediation/2026-09-13/upgrade-consumers-r25/verification.json)记录延期主版本迁移、Cordis 三方重放、真实编译和浏览器验证，以及最终补丁哈希整改。保留这些成果，不重新安装工具链、扫描新版本或重做同一轮升级。当前源码与依赖保持已固定批次。

回执中两个失败集合不同：client 5269/5270 的一项是 `present-open.host` 符号链接权限问题；目录选择器的 Loader 组合另为 15/16，失败断言是界面载入失败后后端未被移除。不要把其中一个归并、忽略或报告为另一个。Loader 的 Vitest 池 CSPRNG 崩溃与直连通过同样分别保留，现有观察不是整个平台或所有配置均无缺陷的证明。审核 PASS 不消除这些运行结果；102 个 Web 文件的汇总与含跳过项的 smoke 记录应分列，不能扩写成所有场景零跳过。

2026-09-13 的 r25 证据目录当前仅有 verification.json；其中原始执行日志定位在本机目录。2026-09-12 的同名目录仍是远端较早修复的日志，不能当成本机终验日志。优先封存已有对应最终命令日志；不存在的如实列为缺失，不重造历史回执，也不只为包装数字机械重跑全库。

## r26 候选一：Loader 创建与移除一致性（附件补丁）

[新增记录](../remediation/2026-09-13/loader-entry-r26/verification.json)对应两个 Loader 源文件和一个测试文件。EntryTree.create 在激活失败时移除候选配置行，并在成功后才发布创建日志。EntryGroup.remove 在永久移除时先解除配置行和成员记录，再等待插件清理；组停止仍保留重启配置，清理失败仍抛出。未修改 Fiber、目录选择器断言、依赖版本、锁或 patch 文件。

[新增方法级测试](../../packages/boot/app-boot/tests/loader-entry-transactions.spec.ts)控制生命周期回调并使用实际 tree/group 操作。作者环境旧代码4过6败、修复后10/10；其中模拟重整回调确实能在旧代码卸载期间重新创建 backend。该局部证据不证明既有完整目录选择器失败已经解决，必须通过真实组合复验。

## r26 候选二：失败启动不能被相同依赖通知重新放行（远端 4d6c7512）

本轮仅改动一个运行时文件：[vendor/cordis/src/fiber.ts](../../vendor/cordis/src/fiber.ts)。启动失败会主动把执行 epoch 改为 INACTIVE，但这不代表依赖真的不可用；原恢复条件因此允许相同依赖的通知清除失败并重启，清理期间也可排入下一次启动。现独立保存失败依赖 epoch，相同依赖保持失败；真正丢失后恢复、更换提供者、显式更新仍可重试，已销毁实例不复活。

[本轮记录](../remediation/2026-09-13/fiber-recovery-r26/verification.json)保存精确基线源码的局部复现及修复后结果：8项控制在原代码5过3败，在修复版8过0败。使用真实 Fiber 全部源码，但反射、事件和堆栈拼接为显式替身；不是完整 Context／Loader／Windows 实跑，也不是严格类型检查。新增[8项正式测试](../../packages/host/directory-picker-auto/tests/failure-recovery.spec.ts)使用实际 Context 和依赖通知，尚待本地执行。

两项核心错误均已局部复现，但均未证明单独或合并即可闭合目录选择器的完整组合失败。原目录选择器测试及其断言一字未改。必须通过该实际入口验证。

## 本地执行的收尾任务

先核对同分支远端尖端与工作区，对照交付清单检查被改文件的原始字节及新增路径，再执行补丁 check/apply；有重叠的新工作就停止覆盖并逐项整合。沿用已安装的 Node26.8.2、pnpm12.4.1、TS7及依赖，不重装、不重置、不自动stash，不再次合并上游。通过正常 Vitest 配置依次执行：新增 10 项 `packages/boot/app-boot/tests/loader-entry-transactions.spec.ts`、新增 8 项 `packages/host/directory-picker-auto/tests/failure-recovery.spec.ts` 与既有 `packages/host/directory-picker-auto/tests/loader-composition.spec.ts` 完整组合。失败用例保留原断言，核验 backend、surface、服务和配置行均不存在；同时验证正常卸载、树先移除、Include写入失败及重载恢复不回归；真实服务恢复、显式更新和正常销毁仍须通过。按影响补 app-boot 的 config-reload、user-patches 及相应 webserver 测试，不用工具包35项代替生命周期检查。

若目录选择器原组合仍失败，保留第一次完整日志及精确条目 ID、对象身份、group.data/store 和通知顺序，再在同分支最小修复。不得删用例、加任意睡眠、改成只请求卸载即算完成、先不创建后端、放宽超时、反复扫除泄漏或回退整批 Cordis。方法级修复和端到端结果分别报告，不把预期结果写成通过。

## Windows 子进程与链接问题

先取回已有 CSPRNG 最小程序、原始 stderr 和直接运行回执。当前只有结构化矩阵可远端读取，不能独立确定根因就是 Node 上游。只在需要区分原因时做一组有限对照：同一程序和参数，在直接运行及真实 Vitest 池中使用实际 Windows 系统目录的合法原生路径、去除大小写重复键后的最小所需环境，保留 PATH 空值及凭据清理边界。不能通过删除 SystemRoot、继承全部环境、关闭随机数／证书或无限延长超时取得通过；未知项保持 BLOCKED。直连五断言通过仍不是池内路径通过，本轮两项核心修复与该平台问题无等价关系。

对 present-open.host 的链接失败，先定位具体测试与目标类型。若断言仅针对目录链接的 realpath 越界，可用真实 junction 验证同一语义并精确解链；文件 symlink 不能用目录 junction 冒充。不得提权或修改系统策略。确实需要文件链接权限时，明确该项未证明，并由已有支持环境执行相同测试；不把整组客户端测试标为全绿。

## 检查、证据与提交

新修复执行受影响的严格类型、lint和文档检查。新 Agent Note 双语核对后仅点名生成其侧车；vendor README 的两个 r26 条目仍是这些行为的拥有者，不改历史版本记录。不得剥除依赖 patch 的空白而不重建其摘要。本轮未改任何依赖锁；必要 frozen 复核以最终已提交字节为输入，锁和补丁未变时复用 91715660 的干净树 frozen 证据并核对 blob。

保存新增用例、原失败复测与受影响回归的原始日志、命令退出码、源码提交／文件摘要；封存 r25 已存在的最终日志，缺失项单列。归档不含账号名、凭据、工具和缓存。本轮实际执行的最终日志必须随脱敏回执归档、可从远端取回。通过的未受影响检查复用原证据，不能将缺少日志自动解释为失败或成功。

继续同一分支正常提交推送，不强推、不合并 master，不改节点通过状态。本轮读取工具不能更新 PR 正文；已有可用工具时采用交付包中的说明更新，没有则只返回已推送 SHA，不以安装 gh 为前置。接收提交的 CI 查询仍有主CI排队和附属流程失败，必须按新提交的实际状态核对，不能以旧成功、排队或跳过替代当前验收。

模型配置两文件、provider/endpoint/思考等级/credentialRef、state.json 和历史证据保持原字节。升级和有限修复授权不等于模型调用或系统初始化授权。不读生产Key及全局认证、不登录、不请求模型、不复制旧沙箱秘密，不改全局工具、共享账号／ACL／Firewall／WFP／UAC，不使用 Remote Desktop Commander、不操作其他项目。三个不可达vendor来源与真实模型／系统授权继续单列，不把这些限制扩大成每轮重复安装、重新配置或新的通用前置框架。
