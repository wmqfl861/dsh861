P0-B 后继规划调用提示词 r01

这是执行者准备、等待所有者批准的输入文本，不是 Codex 计划或审核回执。仅在实际运行授权、固定输入与安全条件都满足后，将本文件原字节交给指定真实 Codex。当前文件本身不授权执行，也不授予 P0-C 准入。

你的任务是以指定真实 Codex 的身份为当前 P0-B 编写有限、可执行、可验收的后继计划。不得更换 my-gpt / gpt-6-astra / max，不修改代码、配置、凭据或历史计划，不发起其他模型调用。计划正文通过受控输出返回，由执行端保存为新版本；不要通过写文件绕过只读执行范围。

只读取本次签名请求 readSet 批准的文件和版本。核心输入为主规格、开发路线、P0-B 的 plan-revision-request.r01/r02、acceptance-map.r01.json、WEB_CONTROL_CONSOLE_SUPPLEMENT.v1.md 及被修订的 plan.v3.md。旧修订请求中的“修复未上传”等描述是历史状态，不得覆盖当前已固定的实际源码与后继回执。必须查源码才能作出的设计判断，仅在该源码已加入获准清单时执行；否则明确列出所需最小追加读取项及其理由，不能擅自扩大范围或编造文件/API。

计划必须保留主规格 AC 原含义：AC-03 为模型/推理配置兑现，AC-04 为同成员跨 harness 的身份/岗位/获准记忆连续，AC-10 为集中 skills/tools，AC-21 为崩溃与交接，AC-23 为取消及资源所有权，AC-24 为预算与限流。节点用例使用独立标识，不能重编号掩盖未完成验收。

分别处理 nativeResume 和 artifactHandoff。不支持原生续接时如实记录；获准的新实例可以通过明确身份、固定输入版本及产物进行真实交接，但不能伪装原生会话或完整 GBrain 连续性。每项必需配置区分 requested、提交的配置、原生观察和 UNKNOWN；字符串回显、模型自述、退出0和模拟用例不能替代真实产品证据。

P0-B 范围应收敛到四种真实 harness 的必要最小适配、配置发现与兑现、运行隔离、允许/拒绝、取消、最小工具连接和可行交接。完整公司控制面、数据库、长期身份/记忆以及公网后台在路线所属后继节点实现，不能全部变成本节点的无限前置；同时不能删除本节点真实产品要求来取得通过。

纳入网页后台、多团队及同团队多任务并行、组织与流程可编辑、任意获准成员对话、全站可折叠 Codex 管理助手的需求影响。本节点只确定后续所需的真实能力和观察方式，不实现整个后台、不假装界面已有交付。

逐项列出节点ID/计划版本、前置状态、需求及AC映射、范围和排除项、核实过的目标文件/接口、实施顺序、可并行但无文件冲突的任务、每项允许/拒绝/异常测试、资源所有者、取消和恢复、实际命令、输入/输出/证据、成功及阻塞标准。固定候选后仍须指定真实 OpenCode 独立硬审核；本次不是该审核。

既有证据只对对应源码和环境有效。复用未受影响的成功证据，后继修改仅复验受影响范围；不机械重做安装、归档或已完成门控测试。不将无 CI 运行记录改写为通过，不把本地或内置子代理 PASS 当成指定审核。区分开发工具使用与交付产品长期隔离，不互相冒充。

无法在获准输入中完成必要核实时，输出 BLOCKED_INPUT，列出唯一缺少的文件/证据/决定，不生成伪完成计划。输入充分时输出计划正文和逐条覆盖对照，明确剩余真实调用、生产授权及不可观察项。无论哪种结果都不得将 P0-B 或 P0-C 标记为通过。
