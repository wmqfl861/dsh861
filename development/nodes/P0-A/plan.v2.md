# P0-A plan v2（对 v1 的有限修订）

**当前节点仍为 P0-A；当前有效计划版本为 v2，由完整的 [plan.v1.md](D:/Projects/dsh861/development/nodes/P0-A/plan.v1.md) 与本修订共同组成。** 仅下述三处冲突以本修订为准，v1 其余内容继续有效。本修订由 ZCode 保存为 `development/nodes/P0-A/plan.v2.md`，无需重写 v1 全文。

本次仅读取指定计划、节点规则和三份状态证据，并对 v1 在内存中计算格式规范化结果；没有修改文件，没有执行安装、构建、测试或代理探针。

**一、允许计划展示副本进行确定性的格式规范化，原始模型输出保持字节不变。**

修订 v1 第 3.1 节、第 7.2 节及第 8 节中“原文不得改写／原样保存”的适用方式：计划的文字、命令、验收要求及历史事实保持不变；项目内的 Markdown 展示副本允许执行下述格式规范化。真实模型输出仍原样保存在：

`D:\Temp_projects\dsh861-node-governance\runs\p0-first-plan-v1.last.md`

该原始证据不得规范化、覆盖或用展示副本替换。

对当前 `plan.v1.md`，采用以下唯一确定的转换：

1. 使用 UTF-8、无 BOM，换行统一为 LF。
2. 将当前 **14 处代码围栏外、行尾两个空格**替换为一个反斜杠 `\`，保留等效的 Markdown 硬换行。这些位置均为计划元数据或任务输入、输出字段；代码围栏内容不变。
3. 文件末尾保留且仅保留一个 LF。除此之外不改文字、段落顺序、命令、参数、路径、验收编号或 v1 的历史状态表述。

本轮读取的项目内 v1 与用户提供的原始输出哈希一致。按上述规则在内存中计算的结果如下：

| 对象 | 字节数 | SHA-256 |
|---|---:|---|
| 规范化前的 v1，与原始输出已记录哈希一致 | 39197 | `a58aea8eb7c54fe08dc3cba40fb697f48e0b5e7da187dee91d49cd10ccbbc572` |
| 规范化后的 v1 展示副本，尚未写入 | 39184 | `8552927515adc430e8e3025f1a8a2eb236bcd42bbf70b3ec7f83c7fc16d3bff7` |

ZCode 落盘时核对转换前后哈希，并在现有计划调用证据索引中补充原始路径、原始哈希、展示路径、展示哈希、转换规则和本修订引用。保留 v1 原有调用证据及 [preflight.json](D:/Projects/dsh861/development/nodes/P0-A/evidence/preflight.json) 中的历史 `plan_sha256`，不得将其回写为展示副本哈希。候选清单记录实际展示文件的哈希，原始调用证据继续引用原始输出哈希。

本次 v2 同样保存原始模型输出、真实调用回执及项目内展示副本，分别记录实际哈希，不覆盖 v1 的任何原始证据。项目内两份计划均按原文档要求接受检查；**本修订不授权忽略文件、放宽预算、关闭规则或跳过其他文档门禁。**

**二、补列两份既有治理文件，允许准确维护状态和工具核验证据。**

修订 v1 第 1.3 节、第 3.2 节、任务 A 第 3 步及第 5、6 节的文件清单和责任说明：原列六份需求、调研、规则文件继续保留，另补列以下两份在基线开始前已经存在的治理文件。

| 文件 | 本节点内的允许用途 |
|---|---|
| [development/NODE_STATUS.md](D:/Projects/dsh861/development/NODE_STATUS.md) | 维护 P0-A 的实际进度、当前有效计划版本、阻塞原因和证据引用；有对应证据后准确记录审核状态。 |
| [development/AGENT_TOOL_VERIFICATION.md](D:/Projects/dsh861/development/AGENT_TOOL_VERIFICATION.md) | 保存开发工具的真实调用核验事实、模型及推理参数证据和引用；必须纳入固定候选的文件清单及内容哈希。 |

`preflight.json` 已包含二者的起点路径、字节数、SHA-256 和保护副本位置，因此这是 **v1 文件清单漏项的补正**。保留这些起点记录和保护副本；后续状态或必要格式修正另记变更后的哈希，不改写起点事实。此前将 `NODE_STATUS.md` 更新为“P0-A 实施中”属于本节点允许的状态维护。

两份文件由 **ZCode 整合者单一维护**。`NODE_STATUS.md` 沿用 v1 对 `state.json` 的后续状态记录规则：单独登记、绑定候选，只记录进度与审核事实；封存时版本和后续状态更新分别可追溯。它不能用于改变已封存的计划、实现或验证证据。工具核验记录及其他固定候选内容发生变化时，仍按 v1 产生新候选轮次。

截至本次修订输入，可以准确记录：起点保护已完成，局部 Corepack 与 pnpm 11.7.0 核验成功，冻结依赖安装运行中，基线说明与双语 Agent Note 的两个实施任务按 v1 范围进行，尚无本节点硬审通过结论。后续进度以实际执行记录更新，不将“运行中”写成“成功”。

本项仅补足治理文件归属和状态维护权限，**不增加产品代码实施范围，也不改变两个文档实施任务的文件所有权。**

**三、将 OpenCode 最小探针更新为已核实的前置事实，保留最终独立硬审。**

修订 v1 第 1.3 节及任务 A 第 5 步的当前前置状态。依据 `development/AGENT_TOOL_VERIFICATION.md` 及其引用的回执、会话导出和模型元数据，接受以下已完成的工具核验事实：

- 真实 OpenCode 版本为 `1.18.27`，探针退出码为 `0`，返回 `OPENCODE_PROBE_OK`。
- 核验会话为 `ses_f81dba69fffeE3FByJaX6941qs`；导出的 user、assistant 记录确认 provider 为 `zhipuai-coding-plan`、model 为 `glm-5.3`、variant 为 `max`。
- 模型元数据确认 `variant max` 对应 `reasoningEffort: max`。

v1 的“探针仍待核实”保留为原计划形成时的历史记录；**v2 当前执行前提采用上述已核实状态**。无需仅为消除这一时间差重复探针。

这些证据证明开发治理工具的真实调用和可观测配置，不构成产品专用 harness 接入、安装隔离、权限或业务验收，也不构成 `P0A-09` 的通过证据。探针没有评审最终候选。

最终仍由真实 OpenCode 使用 `zhipuai-coding-plan/glm-5.3`、`--variant max`，按 v1 规定的独立调用和只读审核方式，对 **v2 有效计划及固定候选**进行硬审。审核输入补充 `plan.v2.md`、计划原始／展示哈希对应记录以及上述治理证据；其余输入、真实调用证据要求、逐项判断和返工复审规则全部保留。

**本修订的执行与验收约束。**

`P0A-01` 至 `P0A-09` 的编号、必需结果、需求及 AC 映射全部保留，不新增验收编号，不放宽通过条件。上述修正纳入原有既有工作保护、文档检查、候选完整性和最终硬审义务。

v1 的命令、参数、测试选择、必选用例、任务依赖及执行顺序继续有效，尤其保留任务 D 的全部检查、随后任务 E 的完整构建和启动验证，以及完成后封存候选、再进行硬审的顺序。`verify-translation-pairing --write`、`test:docs`、`doc-sync`、`lint`、两种差异检查和对未跟踪节点文档的专项核对均保留；专项核对覆盖本次计划展示副本及补列的治理文件。

已完成的起点保护和局部工具核验继续有效；ZCode 接续正在运行的冻结安装并取得真实结果，无需因本修订重新发起安装或重复已完成的前置工作。已开展的基线说明与 Agent Note 工作继续按原范围整合。

**本修订不新增对需求、产品源码、测试及其语义、根配置、依赖版本、锁文件或检查脚本的修改许可。** ZCode 可在现有授权内完成上述三项常规证据修正，无需重复请求授权。只有原定九项验收全部通过，且指定真实 OpenCode 对当前固定候选明确给出 `PASS`，才能将 P0-A 标为审核通过；本修订不规划、不实施下一节点。
