# B02：复用已构建产物，汇合T6与三旅程后完成交付

类型：既有B02收官任务的执行补充；日期：2026-09-21。先读本文件，再读 [LOCAL_AGENT_PROMPT.md](LOCAL_AGENT_PROMPT.md)；[CHECKPOINTS.json](CHECKPOINTS.json) 只描述依赖，不能覆盖本地RESUME。原 [验收](../ACCEPTANCE.md)、[收官手册](../finalization-v1/RUNBOOK.md) 与本地C00正式计划、有效补订继续生效。

## 0. 本地报告、远端观察、当前决定分开

**本地报告：** C12-build已产生 `C:\dsh-b02-c12\product\win-unpacked\`，11886文件、983MB，exe SHA-256仅提供缩写 `a4172ddc…c2ebe96`。desktop-control、workflow-control、ui-control-center、agent-team四包已进入runtime，真实Host组合及前端服务冒烟通过；typert八对、browser-safe 19项、65处JSDoc和三私包pack已完成。构建中修复了 `controlStorageRoot` 必需但组合未提供的问题。T6最终门禁与新产物J1—J3被报告为正在并行。以上不是本包作者的本机实测，也不代表三旅程或C13已经完成。

**远端实读：** PR #13开发HEAD仍为 `59579599fa34b6fff47b1c4d525459d2148eb1f8`，draft，base `feat/multi-agent-company-nodes`不变、未合并；本次查询没有后续评论，固定HEAD下B02运行证据目录返回404。主会话未取得本地构建manifest、`controlStorageRoot` diff、T6或三旅程日志，不能编造它们的完整hash/退出码或据旧HEAD否定本地进展。没有新源码提交，不用旧CI冒称本轮验收。

**本次决定：** 保留正在运行的两条线及现有产物，不重建核心模块、不重跑C00、不重新启动第二总控。现有任务在安全同步点采用本补充；若会话已停止才从真实RESUME接手。真正要完成的是同一候选的门禁/产品/真实能力证据汇合、C13及可用交付，不是再开一轮研究。

回执建议“C13之后再核C10”。执行时应将C10所支撑的A22证据分层与必要获准补验放到C13之前；原finalization-v1的F5已依赖F3/F4，这不是新验收条件。START_HERE的功能和限制正文也须进入审核输入，最后只补实际commit/产物地址等可核对回执，不能审后才改变“哪些Agent真实可用”的实质说明。

## 1. R：接续活动任务，不抢写、不杀进程、不重复构建

限定 `C:\Albert\project\dsh861`、分支 `chore/latest-stable-upgrade-20260912`。先读原run下RESUME、候选索引、活动任务/输出路径及最近日志末段；完整原日志仅在当前问题需要时读取。不扫描HOME/全盘、不依据同名进程猜所有权。核对branch/HEAD/status/ls-remote、已记录PID的启动身份和实际任务状态；远端观察只作核对，不回退本地合法提交。

若T6或J1—J3仍在运行，由现有总控继续拥有它们，不新开同名任务、不并行再跑build、不反复轮询完整日志、不终止它们。没有持续本地执行器时保存明确恢复点，不承诺后台自动继续。完成/失败状态取自真实结束记录，进程存在、消失或一段日志都不能单独补出exit0。

T6只有在不改变J测试所依赖的源码/产物时才可与其并行。测试日志、截图和临时profile用各自目录；不得写入冻结win-unpacked。若T6包含生成器、自动格式化或其他写操作，先由总控限定资源锁/执行范围；发生产品字节变化时保存事实，标注哪些已跑旅程受影响，修复后只重验受影响部分。不要为维持“并行”冒险让两任务使用不同未记录候选。

原keep-local、三个r44原件、r47s及已有交付稿保留；不reset/rebase/amend/stash/clean、重克隆、修改ACL/全局环境或终止用户其他项目任务。已有CP2/prep完成证据复用，缺当前组合覆盖才补，不机械从CP2起点全部重来。

## 2. I：源候选、实际包、门禁和旅程的同一性

先读取既有build manifest/打包日志，确认其源candidate及 `controlStorageRoot` 修复发生在相关pack/打包之前。不能用mtime或exe hash单独推断所有runtime已更新；exe hash只覆盖那个文件，不覆盖另外11885个文件。完整hash缩写不得补造。

复用已有产物清单，必要时仅做一次增量/完整只读核验，覆盖包内应用资源（实际采用app.asar或其他形态）、四包编译输出、依赖、配置/前端资产及链接目标。记录相对路径、类型、bytes/hash、受控链接的目标及是否位于允许范围；不跟随未知junction扫描外部目录、不把模板文件数固定成验收标准。源码TS和编译JS不要求hash相同，必须有可复现构建输入/输出映射；跨不同非确定性构建也不强求目录hash一致。

形成一个轻量关联表：`source_candidate -> build_run -> packaged_artifact_manifest -> T6_input -> J1/J2/J3_artifact -> C10_evidence -> review_candidate -> final_commit`。大包不进源码Git，manifest不含自身，不向包里写证据来改变被测文件。既有同层manifest可直接引用，不另造重复体系。

包中修复确实存在且输入未漂移，就沿用已完成的重型构建和在跑旅程；不为新会话、准备commit SHA、证据文案或审计要求机械重建。只有影响runtime/行为的源码、依赖或配置变化，或无法建立可靠输入/输出对应，才按既有构建流程刷新受影响产物并重验；不能手工替换某个包内文件以绕过正式打包/审核。最终报告区别原始build与后继修订产物。

## 3. S：controlStorageRoot最小修复纳入必需验收

先读实际diff和调用消费者，不猜修复文件/算法；保留原组合失败证据和最小接线理由。按现有正式设计验证，不额外引入新存储/配置方案。

必须覆盖：正常Desktop生产组合提供正确的项目/配置作用域根；关闭重开使用同一合法存储并读回记录；两个原设计要求隔离的项目/profile不会意外落入同一个根；无效/不可写位置或持久确认失败明确报错而非假保存；缺少必需配置时按正式契约明确失败，或仅在已设计的组合边界提供确定默认，不能暗中回退到进程cwd/用户全局共享目录。若复用原C02/C03同候选测试已能证明，直接引用并补一条穿过实际组合的回归，不复制全套用例。

此处是修复验收检查表，不是主会话已经发现其默认路径/权限有漏洞。若根配置位于新公共契约或批准清单之外，按真实范围做最小登记/必要短补订，不等C13才揭示；不得以此重跑72分钟全规划或新增未授权系统配置。

独立子代理在固定diff上检查源码调用链、包中对应运行代码和真实保存/重开证据。仅能启动Host和服务网页是smoke，不替代J1的真实桌面保存、派工与重开。

## 4. 并行流与汇合条件

| 流 | 当前做什么 | 复用/不重做 |
|---|---|---|
| T6，沿用原执行者 | 收集实际最终typecheck/lint/docs/注册测试/组合检查的结果，标明输入candidate、exit、数量与首失 | 已在跑就承接；不是主会话另发第二次全门禁 |
| J，沿用原C12执行者 | 在冻结的新win-unpacked完成J1—J3、实际截图/录像及Session/产物核对 | 一条GUI线顺序运行三旅程；不并行抢同窗口/profile |
| E，轻量证据流 | C10分层核对、获准且必需的实际产品补验；先写START_HERE实质正文 | 可与T6/J只读分析并行；真实新调用需原授权和独占测试资源 |
| 总控/独立复核者 | 核对I/S、处理真实差异、确认所有前置已终结并绑定同一候选 | Git、打包/生成器和公共文件唯一owner；被审文件不继续写 |

J1必须从四个常驻入口组队、配置成员/能力、建任务/依赖、实际派工/后续输入/停止、重开读回，证明目标停止且不影响无关run。J2必须图/表编辑保存并实际运行顺序/并行/独立审核和有限返工，首轮真实受控失败后产物版本改变再审核，错hash不能通过。J3必须固定N次/间隔计划、Host限额/暂停/取消与重开，证明无N+1/超发/重复occurrence，已做不重做，未知动作需核实。具体标准仍按原A01—A24，不删除业务验证。

截图只是界面证据，连同实际Run/Session/持久事件/产物/停止记录方可证明动作。不要用后台写completed、renderer store注入、静态fixture页或开发服务器替代产物路径。模拟时钟用于边界测试，至少保留原计划要求的真实Host结束/重开证据。

E流按每产品实际输出来源区分协议fixture、真实二进制、受管隔离、实际推理请求、桌面全链和取消；keyless既不自动等于真实也不自动等于模拟。现有证据足够则引用，原有效授权覆盖的最小缺口才补真实调用。不读取auth.json/用户.env/Key/全环境，不借开发审核调用冒充交付产品，也不新增费用权限。

C10真实层缺件不妨碍保存已有工程/GUI成果和使用说明，但C13必须看到未满足的A22，不能先给整阶段PASS再事后发现只是fixture。缺件精确标 `REAL_PRODUCT_EVIDENCE_PENDING` 并在最终PARTIAL中说明；不为取得PASS删除必需项，不无意义重复收费审核。

## 5. JOIN：先收敛三类证据，再做C13

进入最终审核前确认：T6适用检查全部有真实结果；J1—J3实际状态已知且输入包身份明确；I/S正确；C10/A22层级与限制明确；START_HERE正文已覆盖四入口/三循环/停止重开和实际可用产品；没有未解释的产品文件写入。普通失败由原责任者在已批准范围内修复、自检、受影响复验和独立复核后继续，无需用户再派单。

若有未满足的必需实际验收，不能称READY_FOR_FINAL_PASS。继续所有可安全推进的动作；真实外部条件剩余时按原流程报告BLOCKED/FEATURE_PARTIAL，审核如被调用也不得把局部质量通过包装为整阶段无条件PASS。

本地已经准备的C13提示词有10个占位符：读取原模板并逐项填写真实值/证据位置，不猜名字、不补缺失hash、不把NOT_RUN写PASS。临调用前检查无未替换token，每个路径存在，candidate含所有新文件和 `controlStorageRoot` 修复，测试/包/旅程映射一致，审核者可按既有只读权限读取。只有实质计划变更才由指定Codex短补订，不重做原计划。

原规则允许的工程检查点可以在必要检查/适用审核后正常提交推送，明确不是最终接受。本补充不新增豁免；若提交须等C13，则用base+完整工作树manifest审核未提交候选，审核后正常提交再验证其树，不为填一个“commit SHA”占位符而绕过规则或造成死锁。使用manifest替代字段须在模板中明确身份类型，不能把它伪写成Git提交。

C13依旧真实OpenCode `--model zhipuai-coding-plan/glm-5.3 --variant max`，实际程序/version/argv/start-end/exit、提示/输入/输出hash与明确PASS|FAIL|BLOCKED共同留证。冻结所有被审产品写入，独立审核不能边审边修；开发审核CLI本身不是A22证据。普通发现本地返工，按变化重建/重验受影响包/旅程并复审。不要在C13之后才进行会改变正确性结论的C10核实或storage修复测试。

## 6. SHIP：一个真实可用交付，不再“等下一轮”

审核所涵盖产品文件、T6、被测包和最终commit必须能对应，不以单exe hash覆盖全包。hooks若改变产品字节按影响重验；仅追加已核实的提交/路径回执不机械重跑所有旅程或完整build。版本信息写入会改变产物时如实记录该构建关系和必要验证，不宣称变动前后全字节一致。

复用一份START_HERE/README-product，给出exe准确路径、文件/包manifest哈希、启动方式与私有数据位置、团队/Agent/工作流/运行中心导航、创建与运行示例、循环限制/暂停取消/恢复、真实可用与未就绪harness、测试模式及已知缺件。正文进入C13；审后只补可核对的最终引用，不后改功能承诺。NSIS/签名/系统安装不作为普通win-unpacked交付的新增前置，不安装新软件或写用户系统配置。

产物目录仍可沿用 `C:\dsh-b02-c12\product\win-unpacked\`，以实际最终位置为准。二进制不进源码Git，只上传到已有授权且确实可用的产物渠道；无在线发布则交真实本机路径/hash，不编造下载链接。截图/视频用安全内容，证据写到原B02 run目录，不另造完整一套档案。

唯一总控正常hooks、明确白名单、非强制快进；不git add全仓，不动keep-local、不强推、不改base/合并、不为回执另造提交。新CI只观察真实run/attempt/head/base/checkout/产物及准备/测试/coverage/skip状态，不借旧B01或不同SHA结果，不手动rerun/cancel。尚未终态或没有读取能力如实标未核验，不无限轮询、不给通过预测。

最终一次报告包含：活动两线最终结果、源候选/修复/产物/T6/J/C10/C13/commit对应关系、原A01—A24、三旅程证据、每个harness层级、START_HERE、实际产物路径/hash、完整commit/parent及两端SHA、push exit、CI、keep-local和真实所有者缺件。全部工作有结论不等于FEATURE_ACCEPTED；沿用原FEATURE_PARTIAL/REWORK状态，不把B01/P0-B、sandbox.yml等旧缺件默认为关闭。

## 7. 最少恢复开销

若原会话仍运行，不启动第二总控；只在其安全同步点采用本补充。若必须换会话，读取原RESUME中当前T6/J进程身份、日志、build manifest、源candidate、未合并证据和next-ready；已经完成的线只收结果，不重新跑。每个旅程/候选切换更新一页索引，不把2000+输出和所有旧计划重复装入上下文。

新的费用/凭据/系统权限、保护面/范围或不可逆发布决定才集中请示。测试失败、已列明的局部修复、证据核对、文案补全和正常收官在本地闭环。保护workflow/vendor、r43/r44核心、原Session代次、全局模型配置与历史证据，不用Remote Desktop Commander、不操作其他项目。本文未读取本机产物、运行测试/模型、修改开发分支或授予新权限。
