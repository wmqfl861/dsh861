# 新本地会话长任务提示词

这是从原C06交接继续到B02交付的指令，不要求重新规划或重新实施九张已接受卡。具体本地路径和候选身份必须来自实际RESUME，不使用主会话猜测。

```text
继续 dsh861 B02。当前本地回执为C00—C06、C09、C11九卡CP1 accepted。
从本地RESUME.json与C06移交的4项C07待办恢复，完成剩余C07、C08、C10、C12、C13。
不要从C00或上一轮“先CP-A1”机械重来，不完成C07就回用户等下一条提示词。

【计划与来源】
新文档分支：docs/b02-c07-to-delivery-20260920
目录：development/delivery-plan/B02/continuation-c06-v1/
固定commit以主会话本次回执为准。
先读本目录README和EXECUTION，再按需查原B02 TASKS/ACCEPTANCE及本地正式计划相应章节。
本地C00正式计划、B02-control-v1、有效补订/授权和原A01—A24/J1—J3继续适用。
本包是执行组织说明，不冒充一次新Codex计划或已通过审核。

【恢复，保留九卡】
仅C:\Albert\project\dsh861，开发分支chore/latest-stable-upgrade-20260912，PR #13草稿、base不变。
核对当前branch/HEAD/status/origin/ls-remote和已记录自有活动任务/push。
主会话远端最后读到59579599fa34b6fff47b1c4d525459d2148eb1f8，
只作为观察，不得据此重置本地后续合法提交或否定本地成果。
保留未提交/未跟踪B02代码及keep-local、三个r44原件、r47s和已有评论稿。
不reset/rebase/amend/stash/clean，不重克隆，不杀任意用户进程或重复启动活跃push。

按旧交接确切路径读取RESUME；缺路径时只在本项目B02运行目录按文件名定位，
用run_id/candidate/指针确认，不只看mtime、不扫HOME/全盘、不创建空恢复文件。
读取当前候选及九卡验收索引，完整读取C06交接4项的原文/目标文件/检查要求。
主会话不知道四项具体内容，不得用自己猜的四条替换它们。
逐项登记原编号、来源、现状、实现/验证，已完成则引用，未完成直接处理。

C09/C11保持accepted，不重写四页或重做全部30项负控。
C07/C08改变它们消费的状态/时序时，只做必要增量接线和受影响回归，
原CP1不代表新字节已审；真实回归仍必须修。
CP-A1先对账：已经接线/检查的引用，缺哪些才补哪些，不因远端未更新而重做十项。
只读取当前计划/接口片段和必要原始证据，不把全部历史日志再装满总控上下文。

【任务安排】
A流：C07→C08，同一scheduler核心只有一个写者。
B流：C10实际受管产品能力/授权验证，恢复后即可并行，不等C08结束才发现缺件。
C流：复用C09/C11做新增循环、限额、停止与恢复的界面和独立测试联调。
默认2实施+1独立审核，资源和原授权允许最多3实施；重型build/打包同时1个。
共享types、Remote、锁、生成器、exports/locale和Git只有总控整合，禁止同文件双写。
某harness缺条件只暂停它的真实验收，继续其他合法独立工作。

【C07】
先落实C06原4项交接，沿已有运行器/准入/feed，不新建另一套状态机。
完成固定N次、审核返工，iteration与attempt分开，审核绑定新产物hash/revision。
达到次数/截止/预算上限明确停止或耗尽，不伪记成功；无界配置Host拒绝。
原子准入同时检查pause/cancel、并发、剩余调用数及截止条件，
两个分支争最后额度最多一个真正调用；重启/未知结果不能随意退回额度绕过上限。
费用未知不显示0，不承诺provider不给数据时精确金额硬封顶。

暂停停止新派发，到真实安全边界才显示已暂停。
取消先关闭新准入，再取消并等待本run自有handles；未确认停下保持取消中/错误，
不能只清前端timer或强退进程；其他run必须继续正常。
验证N+1禁止、FAIL→新产物→PASS、错误审核拒绝、预算竞争、
暂停/取消与派发竞态、持久失败、重复事件及自身等待死锁。
负控复用已有覆盖，只新增缺口；保存字节，finally恢复，按真实行为失败取证。
C07自检、CP1和C09/C11必要CP2后立即进入C08，不回用户派单。

【C08】
按原定义实现固定间隔、次数/结束、下一次时刻、错过策略、不重叠和occurrence去重。
定义/复制模板不得自动启用调度；两窗口/owner通过已有lease/CAS/持久意图协调。
失去lease或取消后迟到timer不能继续派发，不能只靠renderer内存防重。
重启读取真实journal/Session/收据：已完成不重做，未知副作用需核实，
只有明确未发出的步骤安全继续；不假称透明恢复JS堆栈或关机后后台运行。
虚拟时钟边界之外至少一条真实Host关闭重开的持久化链，
验证重复tick/多owner、错过周期、停止、预算不重置、未知动作不重发、资源释放。
通过独立审核和受影响UI/边界联调后推进C12。

【C10与既有UI/测试】
复用C03桥接和workspace隔离，真实可用/未配置/未授权/不支持/未验证分别显示。
通过产品Desktop→Remote→Host→实际受管程序验证，不用开发CLI或mock冒充。
至少一个真实受管执行；跨harness以原A22要求的不同实际产品证明。
只用既有有效授权和安全配置引用，不读Key/auth.json/用户.env，不改全局模型，不接管任意终端。
缺件集中记录，不重复认证/收费试问；工程模拟可以继续但明确标测试模式。

C09接入真正loop/pause/cancel/schedule/recovery及错误/未知状态，
刷新重连不重派、取消中不得提前变已停止，按钮必须改变Host而非本地store。
C11复用风险矩阵与30项负控，增补跨层额度、持久、取消、重启和身份拒绝，
不是再开发两张卡；变化的候选受影响部分要重新验收。

【审核与返工】
每个稳定增量CP1由全新上下文、非实现者检查；每个可运行切片CP2走实际UI/Host。
权限/持久化/公共协议/取消重要变化立即复核，普通问题本地返工继续，不逐条问用户。
原C00/已通过字节证据复用，确有计划实质变更才指定Codex短补订，不重跑全量规划。
所有新模型调用必须在现有有效授权内，不新增费用权限、不换审核者/模型。
最终C13冻结完整候选，真实OpenCode
--model zhipuai-coding-plan/glm-5.3 --variant max
实际argv、程序身份、exit、输出裁决和manifest共同留证，不用CP1之和或prompt自述代替。

【实际产品交付】
C12用新win-unpacked重放原J1组队派工/后续输入/停止/重开，
J2图表编辑/并行/独立审核/有限返工，J3固定与间隔循环/限额/暂停取消/恢复。
从四个常驻页面实际操作，截图/视频关联run/Session/产物，
不后台预造completed再截图，不把旧B01包改名，不只跑development server。
已有工具可构建普通运行目录；NSIS缺失不阻止这个交付，未经授权不安装/签名/发布。

原A01—A24全部给真实状态；实际产品不足就FEATURE_PARTIAL，
不能用“卡全结束、1000+绿”替代功能完成，不能偷偷删四种harness最终要求。
正常hooks、白名单、非强制快进，hooks改产品字节按影响补验。
CI按真实run/attempt/checkout/步骤和artifact读取，旧B01结果不冒充新验收，
不手动rerun/cancel，不为每条日志或最终回执额外造提交。

【避免上下文耗尽】
总控只保留一页状态地图和证据索引；子代理读细节、回结论/差异/路径/hash，
不要把1000项输出和历史完整聊天反复回灌。未变证据引用，不复制成“新审核”。
C07/C08及各切片后更新原run恢复指针，提前保存下一ready任务/owner/候选/活动进程，
需要换会话时据此继续，不把上下文极限当产品完成，也不承诺无执行器仍后台运行。

最终一次报告：恢复来源、C06四项处置、CP-A1实际状态、九卡继承与增量、
五剩余卡、A01—A24/J1—J3、真实产品和keyless边界、Windows产物与使用说明、
硬审/CI、完整commit/parent/本地远端SHA、工作树余项和集中所有者级缺件。
只有新权限/费用/凭据/保护面/不可逆发布决定或无可安全推进工作时才提前集中回报。
保留B01/P0-B真实未闭合项，不改vendor/workflow/r43-r44核心/旧Session/历史证据，
不安装系统服务/WSL/数据库，不用Remote Desktop Commander，不操作其他项目，不合并PR、不进入P0-C。
```
