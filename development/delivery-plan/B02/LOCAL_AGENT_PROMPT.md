# 本地总控启动指令

以下是一条完整阶段任务，不需要用户为每个小任务再转发提示词。

```text
接手 dsh861 的 B02：桌面团队、Agent、工作流和有界循环控制台。
所有者已经明确指出B01桌面功能过少，要求在桌面组团队、操作其他Agent、设置工作流和循环。
本次要交真实产品功能，不是再做一轮底座清理、功能盘点或只打包原聊天窗口。

源码基线：59579599fa34b6fff47b1c4d525459d2148eb1f8
工作区：C:\Albert\project\dsh861
开发分支：chore/latest-stable-upgrade-20260912
PR #13继续草稿，base不变，不合并。

计划在文档分支 docs/desktop-control-b02-20260920，目录 development/delivery-plan/B02/。
准确计划commit以主会话本次固定SHA回执为准，不盲信移动分支。
先读README、BASELINE、PRODUCT_SPEC、ARCHITECTURE、SCOPE_AND_GATES，
再读TASKS、EXECUTION、DEPENDENCIES、ACCEPTANCE。

【接手与正式计划】
核对本地/远端HEAD、status、keep-local和活动写者；保留r48评论稿、r47s及原三个r44文件。
不要求工作树为空，不reset/rebase/amend/stash/clean，不覆盖已完成的B01记录。
计划只读获取并经本地正常检查采用，不切换覆盖开发分支、不重新应用旧S3差异。

由现有明确授权的真实Codex gpt-6-astra/max核对当前节点并形成本需求的正式计划/必要修订。
本包不是Codex运行证据；不从零再研究整个项目，也不只产出一个新计划就停。
用户现在要求产品控制功能，B01只允许预览的旧范围不继续当本次功能禁令；
但不得把P0-B改成通过、不得通过改名绕过正式节点顺序，不启动P0-C数据库部署。
确与旧节点规则冲突的子任务给一次精确调整请求；能在当前节点合法承载的独立工作直接推进。
真实业务harness/数据库缺件只阻断其相关验收，不把整个UI与调度开发一起停住。

【先交可用切片】
按C00—C13：先冻结共享接口，再并行Host/存储、团队Agent UI、流程编辑UI和独立测试。
V1必须在真实桌面创建团队、添加成员、派任务、看输出、发送和停止、保存重开。
V2接通图/表工作流、顺序/并行/分支、独立审核和有界返工。
V3完成间隔调度、预算/暂停/取消/恢复、真实受管产品及Windows产物验收。
不得等所有后端写完才第一次打开新页面；只有页面或只有引擎都不能完成。

【并行与自审】
你是唯一总控、共享契约/生成器/Git整合负责人。
起始2个实施流+1个独立审核流，资源/授权允许最多3个实施流。
同文件只有一个写者；重型build/打包同时1个，profile/home/端口各任务独占。
接口冻结后团队/Agent页、工作流编辑器、Host/runtime和测试按文件分工并行。
每个包CP1新上下文独立审核，每个可运行切片CP2做实际UI/Host组合验证，
关键权限/持久化/循环/取消改变立即审查，本地自行返工继续，不逐问题回用户。
阶段最终固定全部候选，按现有授权用真实OpenCode
--model zhipuai-coding-plan/glm-5.3 --variant max 硬审，不用prompt自述代替。

【关键产品约束】
复用现有TeamService/subagent/Remote/Session，不重写r44生命周期。
Host验证目标项目/成员与exact Agent权限，不能信客户端传的对象。
现有Team仅进程内共享checkout，writeScopes只是警告；不能把它冒充写锁。
共享写者串行或用已验证隔离；审核者只读必须由执行层保证。

工作流定义是结构化图，图/表同源，普通用户不写JS。
明确Loop节点，不允许无界回边、任意eval或while(true)。
支持固定次数、审核失败返工、按间隔重复，每个run有限次数/时间/新调用预算。
暂停只关闭新增派发，取消必须等待自有子任务真实停止，不能清前端计时器假完成。
重启从已持久节点恢复，不盲重发结果未知的外部动作；应用退出不声称后台仍运行。

模板与运行有版本，运行中改定义不改变旧run；存储失败不能返回保存成功。
本地项目/Session控制不等于正式公司数据库；原PostgreSQL/GBrain要求保留，
不得偷偷新建SQLite公司主库或使用localStorage作为唯一运行状态。

Codex/Claude/OpenCode/Grok按真实已配置能力显示；未接入/未授权不假装ready。
实际产品调用只用现有有效授权/安全配置引用，不读Key、全局auth.json或用户.env。
开发CLI能运行不等于交付产品接入通过；keyless测试不冒称真实模型完成。
不接管任意用户CLI进程，不扩大系统权限，不安装NSIS/WSL/数据库，不改全局模型。

【文件和配套】
建议新增desktop-control、workflow-control、ui-control-center三个责任模块，
若已有职责可复用则在C00固定真实路径，不制造空包。
所有真实Remote消费者、workspace引用、生成目录、配对/快照注册先进入一次性文件清单。
允许必要机械接线，不升级现有第三方依赖，不全仓重解锁文件，不拖到最终提交再问S3。
旧B01证据、模型配置、vendor、workflow、r43 observer和既有Session代次冻结。
确需改保护面时给最小具体原因和验证，不用暂停整个批次代替范围分析。

【交付标准】
按ACCEPTANCE的A01—A24和J1—J3验证；必须在新的win-unpacked里直接操作。
新增四个入口和真实Host行为，不用后台脚本预造成功数据截图。
至少一个实际受管执行，跨harness能力用两个不同已授权产品证明；缺件如实PARTIAL。
安装器与签名不是这批功能的必要前置，复用B01已有打包工具产出可运行目录。
既有安全/安装/正式P0缺件继续记账，不宣称整个项目完成。

正常hooks、白名单、同分支非强制快进推送；不要每个小文案触发一次新CI。
同一失败无新证据不重复收费规划/审核。额度或环境受限先继续其他独立ready任务。
上下文中断保存delivery-runs/B02中的candidate、状态、owner、活动任务和恢复说明，
新代理按实际记录续接，不重做已完成工作，不承诺无执行器仍在后台。

最终一次回传：真实新增操作、A01—A24、三旅程视频/截图与Session/产物证据、
每个harness的实际就绪/限制、真实/模拟验证区别、完整commit/两端SHA、
新Windows产物位置/hash/启动说明、CI及集中所有者级缺件。
状态为FEATURE_ACCEPTED / FEATURE_PARTIAL / REWORK，
四个核心能力缺实际桌面路径时不得用“任务全终结、工程全闭环”代替完成。
```
