# B01 并行化分析：还有哪些模块/节点可以同时开发

类型：只读调度分析。运行：B01-20260919-01。日期：2026-09-19。
回答所有者问题："根据蓝图，还有没有并发可以同时开发的模块或节点？"

依据（本轮全部实际读取）：`plan/formal-plan.v1.md`、`STATUS.run.json`、delivery-plan 的 `PRODUCT_BLUEPRINT.md` / `REQUIREMENTS_MATRIX.md` / `DEPENDENCIES.json` / `PHASE_B01.md` / `SCOPE_AND_AUTHORITY.md`、`W09/requirements-matrix.b01.md`、`W09/reuse-and-gaps.md`、`W10/p0-b-readiness.md`，及运行根目录列表核对（W03/W07/W11/W12/W13 目录尚未创建，与 planned 一致）。

本文件未启动任何工作、未调用任何模型、未读取密钥/.env/auth.json、未修改任何其他文件。**本文件是分析，不是批准**：凡超出 B01 现有 14 张卡（W00—W13）的项，一律标注"需所有者明示"。

## 1. B01 内部：14 张卡的即时并行性

当前调度态（STATUS.run.json）：accepted = W00/W01/W08/W09/W10；wait_ci = W04（CP1 local_pass，终验收等 W03 Linux 证据）；running = W02/W05/W06；planned = W03/W07/W11/W12/W13。

逐张核对未启动的 5 张卡（对照 DEPENDENCIES.json 的 start_after / conclusion_only 与 formal-plan §4.1）：

| 卡 | 开始条件（formal-plan §4.1） | 当前判定 |
|---|---|---|
| W03 | W02 通过 + CP-A 发布候选可用 | 阻塞：W02 在跑，CP-A 候选未形成。W03 本身只读 CI 证据、不占源码写入槽，但条件未满足 |
| W07 | W06 accepted | 阻塞：W06 在跑。另注意 STATUS 证据中的 INCIDENT W06-1（fixture 注入开发项目、真实 client 启动失败，W06 曾冻结待返工）：W06 验收时点后移，W07 与 A5/A6 关键路径随之延长 |
| W11 | W03 accepted 或有证据的阻塞/延期结论 | 阻塞：W03 尚无任何结论；conclusion_only 只免除"accepted"，不免除"有结论" |
| W12 | W03—W11 全部有完成或证据支持的结论 | 阻塞：汇聚节点，上游未齐 |
| W13 | W12 固定候选及结论 | 阻塞：串行末段 |

已启动未结束的卡（W02/W05/W06）文件面互不重叠（prepare spec/test-support 锁 vs SDK 快照/expected/identity 锁 vs Desktop 源码/runtime 锁），与 DEPENDENCIES 不变式"W01/W02 不同时写 spec"及快照锁"W04→W05 一方持有"一致；W04 的锁已按冻结交接移交给 W05，W04 只欠 W03 证据与 W05 修复后的补验，不需要也不能占一条新实施流。

**结论：B01 内部 14 张卡中，除已在跑的 3 条流外，没有任何可立即并行的卡——明确回答"无"。**

关于并发上限：formal-plan §4.2 定实施流"起步 2，最多 3"，当前 W02/W05/W06 三条实施流已达到该上限。STATUS 证据同时记录了所有者 2026-09-19 指令（"独立即最大化并行、回收即派发下一就绪任务"，曾以 W04+W08 将流数提到 5）——即所有者意愿下的实际上限高于计划值 3。但当前瓶颈不是槽位而是**依赖就绪**：就绪队列为空，给再多槽位也无卡可派。因此"3 流在跑"就是现阶段 B01 的合理并发形态；下一步自动解锁点是 W02+CP-A（→W03，并连带 W04 终验收、W11）与 W06 验收（→W07）。

## 2. B01 边界内的灰色区：W09 盘点项里有无可并行的"纯准备"

口径：纯准备 = 不写产品源码、不触密钥/凭据、不违反 PHASE_B01 非目标（不实现公司控制服务、不建 PostgreSQL/GBrain 实例、不启动 P0-C、不为四种真实模型申请新账号、不签名/正式安装包）、且不与在跑流冲突。另受两条例外约束：W09 已 CP1 accepted 冻结，重做同类盘点违反 SCOPE §6"同一阻断原样不变不重复探测"；formal-plan §4.2"产品盘点仍计入实施/分析调用预算，不凭只读无限扩代理"。

### 2.1 reusable_code 8 项（AC-04/05/06/10/19/24/26/32）逐项判断

W09 对这 8 项已完成"实现位置 + 真实消费者 + 局部测试"的 blob 级绑定（附录 A 证据）——即任务设想的"消费者验证"**已经做完并被 CP1 接受**。再往下每项只剩两类，均不可在 B01 内自动开：

| AC | 对应可复用件 | 剩余工作的性质 | 判定 |
|---|---|---|---|
| AC-04 | R1/R4（subagent seam、continuation、ACP 后端） | 跨 harness 身份/记忆连续的真实环境验证 | P0-B/P3 验证，需真实 harness 授权；B01 内无纯准备可做 |
| AC-05 | R10（agent-team，实验） | 独立工作区设计 + 两成员并行产物验证 | M1c+ 设计/验证；设计草稿见 2.3（需所有者明示） |
| AC-06 | R7（system-prompt/agent-instructions/hooks 桥） | 规则管理台/版本/冲突报告（G7） | M1c 设计草稿属灰色区（见 2.3）；无 B01 内验证可做 |
| AC-10 | R8（skill 注册表/MCP 桥） | 岗位/项目/实例级授权维度 + GBrain 接法（G5） | 依赖 P0-C 与 embedding 授权；接法设计草稿属 2.3 |
| AC-19 | R9（workflow 引擎/plan/todo） | 结构化编辑、流程图、模板版本 | M2b 设计；AC-19 行明示"不在本批次越过 P0 实现 P1/P3" |
| AC-24 | R13（token-meter/retry/jobs 上限） | 费用上限动作层 | M1c/M3 设计草稿属 2.3 |
| AC-26 | Desktop 插件管理链 | 版本路由/升级整体验收（G9） | M3/P5；设计草稿属 2.3 |
| AC-32 | R15（benchmarks 五套） | 参考环境代表性任务集（G13） | 需参考环境；任务集定义草稿属 2.3 |

### 2.2 unassessed 8 项（AC-08/09/11/12/20/22/25/27）逐项判断

这 8 项的状态依据是 W09 的"已检索 tracked 源码无实现"（方法见 W09/00-start §2），属已完成的负向盘点。所谓"证据补盘点"只有两种走向，均不成立：(a) 更深检索 = 重做已接受的盘点，违反不重复探测；(b) 补实现 = 产品源码开发，直接违反 PHASE_B01 非目标与各 AC 行的阶段归属（P1—P6）。AC-12 的附注（`website/` 是产品自身文档站而非业务网站）、AC-25 的相邻能力（Desktop 整包更新 ≠ 运行中升级）均已记录在案，无可增量。**判定：8 项均无 B01 内纯准备可做。**

### 2.3 灰色区真正剩下的候选（全部超出 14 卡，需所有者明示）

**A. G14 文档失配修正（唯一称得上"纯准备"的实物工作）**
- G14-①（= W10 S1）：根 `AGENTS.md` 布局表列有 `packages/self-modification/` 但目录不存在。修正为删行或补目录二选一，过 `doc-sync`。不写产品源码、无密钥、与在跑流零文件冲突。但根 AGENTS.md 不在任何 B01 任务的 FILE_OWNERSHIP 白名单内（W08 白名单仅事件表三件 + 新 spec），且它是代理行为指令文件——**技术可行，需总控登记 + 所有者明示后由文档责任者处理**；注意 `doc-sync` 属"全量文档检查"，按 formal-plan §4.2 计重型资源，须避开 W06 占用的 heavy-build（见第 4 节）。
- G14-②（= W10 S2）：`subagent-codex` README 写 pin 0.153.4、实际 0.154.0。W10 已明示**不单独改字**——README 声称的升级验证证据是否覆盖 0.154.0 不在案，修正须与 P0-B 复核该证据覆盖度一并进行。判定：现在不动。

**B. 后继阶段设计草稿（W09 G 表"现在能继续的任务"列）**
W09 对 G6/G7/G8/G11 标了"已有有效授权？是（设计类）"，这是对**授权性质**的判断（设计无外部依赖、不触密钥），不等于 B01 已排入这些工作——它们不在 14 张卡内。逐项：G6 成员身份/岗位授权模型草案、G7 规则管理模型、G8 外部副作用幂等状态机、G9 版本路由方案、G11 控制台语义层原型、G12 备份清单、G13 任务集定义、G1 ACP 通道可行性只读分析、G2 Grok 版本固定方案、G4 数据契约只读讨论、G5 MCP 桥接法设计、G10 三端模板/脚手架选型只读分析。
共同性质：只读/设计、无文件冲突、无凭据需求；但 (a) 超出 14 卡；(b) 属后继节点规划的输入材料，而蓝图 §4 与 REQUIREMENTS_MATRIX（AC-19 行"不在本批次越过 P0 实现 P1/P3"）要求后继详细规划由指定真实 Codex 在前置节点通过后制定——内部草稿只能作参考输入，永远不能替代正式计划；(c) 消耗实施/分析调用预算。**判定：可行但全部需所有者明示；若开，建议限定为"参考材料"定位并写明不作正式计划。**

### 2.4 小结

B01 边界内、不经所有者新指令就能并行的纯准备：**无**。最接近的是 G14-①（一行文档修正），也因白名单外而需登记与明示。设计草稿类是所有者如果想提前并行的最低成本选项（第 3 节给出每项最小输入）。

## 3. 蓝图后续阶段的提前启动条件（全部需所有者明确授权）

总门闩：P0-B `state.json` 为 blocked、`next_node_allowed: false`（W09 E68）；PHASE_B01 非目标"不启动 P0-C"（E69）；蓝图 §4 明示后继行是架构与路线，不是让执行者在 P0-B 未通过时预先规划/实施 P0-C—P6 的命令。因此下表任何一项"提前并行"都需要所有者**同时**给出：(a) 对该项的节点顺序例外的明确指令；(b) 该项自身的最小输入。两者缺一即停在请求阶段。

| 模块/节点 | 蓝图归属 | 被什么卡住（引用） | 若要提前并行的最小授权/输入 | 当前状态 |
|---|---|---|---|---|
| P0-B 实施验收链本体（C7） | M1a | C1—C6 全部未解除（W10 §1.C） | W10 §2 五项最小请求：①凭据（撤销+替换暴露 Key，SecureString 录入 `dsh861/providers/codex`，单次调用绑定）②路由（HTTPS base URL + 新 models.v1 授权版本，或单次明示接受现行 HTTP）③额度（费用上限数值/币种、有效期、deadlineMs/terminationGraceMs/maxChannelBytes 与金额强制点）④签署（批准规划启动申请 r01 固定输入集）⑤系统影响（Windows elevated 清单精确批准，或明示维持现状接受差距）。之后仍须指定 Codex plan.v4 → 实施 → 指定 OpenCode 硬审 | 未批准；不阻塞 B01 底座任务 |
| OpenCode 适配器 | P0-B（AC-01 3/4） | G1：无产品接入授权；PHASE_B01"不为四种真实模型申请新账号" | OpenCode 程序固定版本 + 受管安装 + 启动/IO/取消/身份方案 + 账号授权；外加 P0-B 放行（或所有者明示的顺序例外）。只读的 ACP 可行性分析见 2.3-B | 未批准 |
| Grok 适配器 | P0-B（AC-01 4/4） | G2：版本解析（仅 REQ-004 来源命令）、安装通道、账号全缺 | `@xai-official/grok` 固定版本解析 + 安装通道 + 账号；同上顺序例外 | 未批准 |
| 受管 PostgreSQL + GBrain（AC-16/17/18/29/30 前置） | M1b / P0-C | G4/G5：P0-B next_node_allowed=false（E68）+"不启动 P0-C"（E69）；无 embedding/重排/综合模型授权 | 顺序例外 + Linux 节点与 PG 部署（平台库/GBrain 库独立账号 + pgvector）+ embedding 等模型调用授权 + source↔成员身份映射设计输入 | 未批准；本机不装数据库 |
| 公司控制台/身份/规则/幂等（AC-04/05/06/08/09/20/22/31 后半） | M1c / P1 | 前置 P0-C 未放行；公共数据/状态契约未冻结（蓝图 §3"接口先定后并行"） | 所有者明示的设计草稿授权（2.3-B，零外部依赖可即刻开）；正式实施仍须节点链 + 契约冻结后控制台/适配/权限/测试四路并行 | 未批准 |
| 三端交付：网站/Android/iOS/微信小程序（AC-11—15） | M2c / P4 | G10：工具链未登记、AppID/签名/上传/设计工具授权全缺；前置 P1/P2 | Android 工具链节点与构建授权；macOS/Xcode 节点或授权构建服务（iOS 签名前置）；微信 AppID + 上传授权；图像服务或本地设计工具授权。规格明示分列，缺条件不冒充上线 | 未批准 |
| 组件版本共存/路由回退、备份恢复（AC-25/26/27/30） | M3 / P5 | G9：版本路由为新增设计、Desktop 明示无自动回滚；G12：独立恢复环境缺 | G9 设计授权；G12 需独立恢复环境 + RPO/RTO 实测窗口 | 未批准 |
| 性能任务集与发布准备（AC-32、P6） | P6 | G13：参考环境（8vCPU/16GB/SSD）未建；签名/外部发布须具体授权 | 参考环境 + 代表性任务定义 + 费用采集口径；发布/签名/采购逐项授权 | 未批准 |

W10 §1.A/§1.B 的历史阻断与已解决开发工具条件（H1—H3、D1—D5）不改变上表：它们证明的是"曾经的工具/认证问题已被更晚记录取代"，不构成产品验收，也不构成任何上项的提前授权。

## 4. 资源与互斥核对

任何"建议并行"的项须过下面这张占用表（formal-plan §4.2 + DEPENDENCIES defaults；D12：typecheck/lint 内含 Host 构建，全量文档检查同计重型）：

| 资源（额度） | 当前占用 | 排队/后果 |
|---|---|---|
| 实施流（计划上限 3；所有者指令下曾达 5） | W02、W05、W06 | 无就绪卡可派；槽位不是瓶颈 |
| 重型 build/打包/全量文档检查（1） | W06（Desktop 构建） | W07 在 W06 后接同一资源；G14-① 所需 doc-sync 也属此类，须等 heavy-build 空闲 |
| 轻型测试进程（2） | W02（windows-test-slot）、W05（expected/snapshot 运行） | 两槽趋满；任何新增本机验证须排队 |
| 共享生成器（1，仅总控） | 空闲（W08 已 accepted，无待写回） | 保持总控串行；无新需求 |
| Desktop runtime（1） | W06 | W07 独占接续 |
| Git 写入（1，仅总控） | 空闲 | CP-A 候选形成/推送前的窗口 |
| 文件写锁 | W02=prepare spec/test-support；W05=SDK 快照/expected/identity；W06=Desktop 源/测试/脚本 | 三组互不重叠；W03/W07/W11/W12 均只消费这些面的产出 |

灰色区候选项的冲突核对：2.3-A/B 全部为只读或设计产物（落运行目录或既有文档），与 W02/W05/W06 的写面零交集；唯一资源代价是分析/子代理调用预算（计入实施预算）与 G14-① 的 doc-sync 重型检查时点。结论：**文件互斥层面没有阻碍灰色区项的因素，阻碍全部是授权层面（超 14 卡）**。

## 5. 结论表与诚实边界

| 项 | 现在可并行？ | 定性 | 若要开，需要什么 |
|---|---|---|---|
| B01 内第 4 条实施流（14 卡内新卡） | 否 | 无就绪卡；W03/W07/W11/W12/W13 全部被 start_after 卡住 | 无——等 W02+CP-A 与 W06 验收自动解锁 |
| 当前 3 条实施流（W02/W05/W06） | 维持 | 已达 formal-plan 上限 3；所有者指令下上限更高但无卡可派 | — |
| G14-① 根 AGENTS.md 布局行修正 | 灰色（技术可行） | 唯一实物级"纯准备"，但在 14 卡白名单外 | 总控登记 + **所有者明示**；doc-sync 避开 heavy-build |
| G14-② subagent-codex README pin | 否（现在不建议） | W10 S2：须与 P0-B 证据覆盖复核一并，不单独改字 | **所有者明示** + P0-B 复核窗口 |
| 后继设计草稿（G1/G2/G4—G13 各"能继续"项） | 灰色（可行） | 只读/设计、零文件冲突，但属后继节点规划输入且超 14 卡 | **所有者明示**（可即刻开、成本最低的提前并行选项；产出只作参考材料，不替代指定 Codex 正式计划） |
| P0-B 解锁链 | 否 | C1—C6 五项 owner 输入未齐 | W10 §2 最小清单（凭据/路由/额度/签署/系统影响） |
| P0-C 及以后任何实施（PG/GBrain/三端/回退/性能/发布） | 否 | E68/E69 节点门闩 + C1—C7 + G4/G5/G9/G10/G12/G13 | **所有者明示**节点顺序例外 + 上表逐项最小输入 |

诚实边界：本分析没有把任何"可以做"说成"已批准"。B01 现有授权覆盖的只有 14 张卡，其中可并行的都在跑；所有者"最大化并行"指令提高的是就绪任务的派发速度与流数上限，不扩大工作范围。灰色区与后续阶段的每一项，在所有者明确点头之前都保持未启动；本文件亦未启动它们。
