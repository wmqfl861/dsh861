# 项目节点开发与硬审核规则

生效日期：2026-09-08。来源：用户当前会话的明确安排。

本规则适用于本仓库多智能体软件公司项目的开发过程，不等于交付产品里所有岗位的永久 harness 绑定。需求、阶段路线与产品验收分别以 [主规格](MULTI_AGENT_REQUIREMENTS.md)、[开发路线](MULTI_AGENT_DEVELOPMENT_ROADMAP.md)和其中的 AC 条目为依据。

## 1. 明确分工

| 环节 | 执行者 | 本项目固定调用设置 |
|---|---|---|
| 当前节点详细计划及必要的计划修订 | 真实 Codex CLI | 模型 `gpt-6-astra`，思考等级 `max`。 |
| 按已成文计划实施、测试、整合和修复 | ZCode 主执行者，可委派独立实施子任务 | 用户本次已明确授权 ZCode 开发和根据审核意见修复；未授权改动其他模型配置。 |
| 当前节点完成后的硬审核及返工复审 | 真实 OpenCode CLI | 模型 `glm-5.3`，思考等级 `max`；本机已发现的合格模型标识为 `zhipuai-coding-plan/glm-5.3`。 |
| 范围、模型、凭据、治理或不可逆动作的额外决定 | 用户 | 沿用现有有效授权，不把超时或沉默作为批准。 |

这项当前会话安排替代此前未指定执行者的开发流程。OpenCode 是本项目节点硬审核者，不自动追加 Claude Code 为必经节点，也不将 Claude Code 调用规则移植到 OpenCode。今后若实际调用其他产品，仍遵守其适用的用户规则。

## 2. 真实调用与参数证据

Codex 与 OpenCode 必须通过真实 subprocess 调用其可执行程序，记录绝对路径、程序版本、模型参数、思考等级、开始结束时间、退出码、提示词摘要哈希及输出位置。内置 Agent 只能如实标为内置实施/检索子代理（用户惯例称 GLM 子代理），不能冒充 Codex 计划或 OpenCode 审核。

Codex 使用本次调用参数 `--model gpt-6-astra` 和 `-c model_reasoning_effort="max"`，OpenCode 使用 `--model zhipuai-coding-plan/glm-5.3 --variant max`。不能只在提示词中写模型名或将 `--thinking`（显示思考内容）当成思考等级；需要核实 CLI 和提供商对参数的实际支持。

模型或 `max` 不受支持、认证不可用、输出未生成、执行失败或运行身份无法核实时，当前节点标记阻塞，不能降级、换模型、伪造回执或跳过审核。全局模型/provider/endpoint/Key 等配置不因本规则自动被改写；仅采用当前用户已明确授权的调用级模型与思考参数。

用户要求的 `src.ops.real_agents` 既有实现经限定范围检索未找到。本次调用包装位于 `D:\Temp_projects\dsh861-node-governance\src\ops\real_agents.py`，是本轮新建的临时 subprocess 包装，不是找回的原实现。它调用真实二进制并记录结果；不得将此说明替换为“沿用了已存在的调用模块”。临时提示词与原始输出也放在该语义化目录下，节点正式产物保存到项目中。

本项目调用既有 CLI 是开发工具使用；交付产品中的专用 harness 安装隔离仍须另行实现和验收，不能用开发工具位置冒充产品隔离已完成。

## 3. 节点顺序

节点依次经历：`待规划`、`计划就绪`、`实施中`、`自检完成`、`待硬审核`、`返工`或`审核通过`。任何环节不能满足前置条件时进入`阻塞`并保留原因。

1. 从已通过的上一个节点结果确定当前节点范围；第一个节点属于 P0。
2. 调用指定真实 Codex 阅读主规格、路线、补充追踪、相关评估和已通过节点证据，编写当前节点详细计划。
3. 保存 Codex 原始计划和调用证据，检查是否包含可执行步骤及可验证的完成标准。计划有缺项或越权要求时交回 Codex 修订，不由实施者悄悄改变范围。
4. ZCode 按该计划实施并整合可并行子任务，运行计划中的适用检查，记录未运行项目和真实限制。
5. 固定本次候选变更和证据，调用指定真实 OpenCode 对照计划、需求与实际文件进行硬审核。
6. 未通过时逐项修复并补充验证，然后再次调用同一指定审核产品和设置复审；任何修改都使原候选审核状态需要重新确认。
7. 只有 OpenCode 对当前候选明确给出 `PASS`，且必需检查实际通过，才可开始下一节点的 Codex 详细规划。不能提前规划或实施下一节点来绕过顺序。

P0 至 P6 是阶段，阶段可以由 Codex 在当前计划中拆成可独立验收的顺序节点。一次只激活一个节点；小节点的完成不能被描述为整个阶段已通过。节点范围不得为了得到通过而删除主规格的必需验收要求。

## 4. 详细计划的最低内容

每份计划标明节点 ID、版本、前置节点、需求和 AC 映射、范围和明确不纳入本节点的范围。内容包括经过源码核实的目标文件、必要接口和数据责任、实施步骤、测试输入与命令、预期结果、失败及恢复处理、产物路径、完成标准和可并行任务划分。

涉及身份、权限、数据、模型输入、工具或生命周期变更时，明确真实组合、拒绝路径、持久状态和会话快照的验证方式。不能只写“加测试”“适当处理错误”等不可验收表述。对缺少凭据或平台的验证要写清阻塞条件，不能把跳过等同通过。

计划修改仍由真实 Codex 按相同模型设置完成，记录变更原因和对已做工作的影响。实现者可以修复计划范围内的问题，但不能自行改写验收标准来关闭失败。

## 5. 并行实施规则

在同一节点内尽量并行处理无写入冲突的代码、测试、文档或只读调研。每个子任务明确输入、输出、文件所有权、依赖与验收命令；未完成的前置接口不能靠各自猜测实现。

共享 schema、锁文件、根配置、迁移及中央类型文件由一个明确责任者修改，其他任务提供需求或补丁建议。不同子任务使用隔离工作空间或互斥文件范围，最终由 ZCode 整合并运行组合测试。

并行子代理的自检不能替代 OpenCode 节点硬审核。不能并行执行当前节点的实现与最终审核，也不能在最终审核进行时继续变更待审核文件。审查发现问题后进入下一轮候选，不修改已经保存的审查回执。

用户要求“尽量多并行”以独立工作真实存在为前提，不生成无必要的重复子任务；模型选择不可控的内置工具不得冒充已指定模型的外部 CLI。

## 6. 硬审核与通过标准

审核输入包含当前节点计划、固定候选文件清单及内容哈希、需求映射、变更内容、测试命令和输出、产物、运行证据及前轮问题闭环。包含新增未跟踪文件，不能只审查 `git diff` 能看见的内容。

OpenCode 必须核查实现是否符合每条节点验收、是否违反主规格、真实调用与权限是否可信、文档是否匹配、测试是否实际运行及足以证明行为，并识别数据丢失、并发和恢复缺口。审核者不得直接修改实现；需要重新运行验证时使用获准的只读检查或由实施者提供可复现环境，缺乏必要证据即不通过。

审核输出保存为结构化结果和可读说明，至少包含节点、计划版本、候选哈希、实际 model/variant、`PASS|FAIL|BLOCKED`、逐项验收结果、发现的问题及证据位置。传参证明与运行结果分开，不能只凭模型自述其身份作为调用证据。

`PASS` 要求所有本节点必需项有实际证据、没有未解决的阻断或正确性问题、没有以跳过代替成功的必需检查。低优先级且不影响本节点约定的后续改进必须明确列出，不能被用来隐藏未达标项。只有 CLI 退出零而没有明确可核对的审核结果，不算通过。

`FAIL` 必须返工，`BLOCKED` 必须解决缺少条件或向用户报告；不能把失败重命名为建议后直接前进。修复重复失败时仍保留每轮问题和结果，不以停止重试代替通过；遇到只有用户能决定的真实阻塞才暂停。

## 7. 节点产物与变更纪律

正式节点计划、状态、候选清单、验证摘要和硬审记录置于 `development/nodes/<节点ID>/`，保留每轮版本；命令原始输出及临时试验在 `D:\Temp_projects\dsh861-node-governance` 及对应语义化目录，长期验收所需证据以脱敏文件或哈希引用归档。

节点通过状态与候选哈希绑定。修复、合并并行子任务或修改配置后重新核对候选，不能复用旧回执。若前节点的后续修改破坏已通过行为，重开受影响验收，不让后节点掩盖回归。

正式代码在开发分支执行，遵循 [AGENTS.md](AGENTS.md)、[测试政策](docs/testing.md)和相关文档规则。非平凡变更带 Agent Note、配套说明及所需快照；不得削弱测试或权限限制来得到通过。

本规则授权启动开发和本节点所需可逆实施，未额外授权 git 提交/推送、外部发布、采购或破坏性迁移。全局模型配置和不属于系统的进程资源继续受用户约束保护。
