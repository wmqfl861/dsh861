packages:
  - vendor/*
  - packages/*/*
  # The Landlock launcher is developed with its harness consumers but keeps
  # its native build and publication scripts under native/system.
  - native/system
  - native/system/packages/*
  # Product assemblies over the package tier; apps/cli owns the `dsh` bin.
  - apps/*
  # Private package owning repository-level benchmark dependencies.
  - benchmarks
  - website
  # Deploy root of the single-exe build: a pure dependency manifest whose
  # closure is what the exe bundles and what the Python runtime distributes.
  - python/sdk-runtime

# Vendored framework packages keep their upstream semver ranges, while local
# builds must resolve those matching names to this workspace's pinned sources.
linkWorkspacePackages: true

overrides:
  '@deepseek-ai/cosmokit': 'link:vendor/cosmokit'
  '@deepseek-ai/schemastery': 'link:vendor/schemastery'

peerDependencyRules:
  allowedVersions:
    typescript: '>=5 <7'

# pnpm 10+ blocks any dependency shipping an install/build script until it is
# explicitly reviewed here (strictDepBuilds defaults to true: an unlisted script
# is a hard install error). Every such package MUST be listed; we deny by
# default and only allow scripts we need. esbuild (native binary) and lefthook
# (git hooks) genuinely need theirs.
allowBuilds:
  esbuild: true
  lefthook: true
  # Cross-platform boundary for the persistent PTY backend, including ConPTY on Windows.
  node-pty: true
  # Pulled in by @earendil-works/pi-ai (optional LLM API backend). pnpm lists
  # them only because they ship lifecycle scripts, but those are no-ops we don't
  # need, so we deny them — install still succeeds.
  '@google/genai': false
  protobufjs: false
  node-addon-require-builtin: false
  # JSONL durability calls MoveFileExW with write-through publication on Windows.
  koffi: true
  # The Python runtime deploy includes the reviewed workspace postinstall that
  # restores the executable bit on node-pty's macOS spawn helper.
  '@deepseek-ai/dsh-subprocess-local@file:packages/subprocess/subprocess-local': true
  # electron-builder pulls in the optional Squirrel.Windows helper, whose
  # install script only selects its bundled 7-Zip executable. Desktop ships
  # Windows through NSIS, so that mutation is not part of our build.
  electron-winstaller: false
  # Store-index rewriting only needs msgpackr's portable JavaScript codec.
  msgpackr-extract: false
  '@anthropic-ai/claude-code': true
  # Project-local dev-tool CLI install scripts, reviewed 2026-09-12: the
  # claude-code and opencode postinstalls only copy the matching platform
  # binary from optionalDependencies over the package's own bin/ placeholder
  # (no network, env or user-home writes), so they run.
  opencode-ai: true
  # Grok's postinstall propagates the binary into ~/.grok/bin — a user-home
  # global location. Denied: package and platform binaries still install
  # into node_modules for byte verification, but nothing may write outside
  # the project tree.
  '@xai-official/grok': false
  # Desktop's bundled package-manager devDep: its reviewed install script only
  # relinks the package's own bin placeholders to the host's native pnpm
  # binary inside the install directory; when blocked it falls back to the
  # Node-interpreted placeholder.
  pnpm: true

minimumReleaseAgeExclude:
  # Fresh pi-ai releases carry the model catalog updates that are the whole
  # point of bumping it; waiting out the release age would defeat that.
  - '@earendil-works/pi-ai@0.85.1'
  - '@earendil-works/pi-telemetry@0.85.1'
  - node-addon-native-custom-loader@0.1.4
  - node-addon-require-builtin-darwin-arm64@0.1.4
  - node-addon-require-builtin-darwin-x64@0.1.4
  - node-addon-require-builtin-linux-arm64-gnu@0.1.4
  - node-addon-require-builtin-linux-x64-gnu@0.1.4
  - node-addon-require-builtin-win32-arm64-msvc@0.1.4
  - node-addon-require-builtin-win32-ia32-msvc@0.1.4
  - node-addon-require-builtin-win32-x64-msvc@0.1.4
  - node-addon-require-builtin@0.1.4
  # The active pnpm supply-chain policy blocks this reviewed runtime closure
  # until its release-age window expires unless every exact package is exempt.
  - '@anthropic-ai/claude-agent-sdk-darwin-arm64@0.3.269'
  - '@anthropic-ai/claude-agent-sdk-darwin-x64@0.3.269'
  - '@anthropic-ai/claude-agent-sdk-linux-arm64-musl@0.3.269'
  - '@anthropic-ai/claude-agent-sdk-linux-arm64@0.3.269'
  - '@anthropic-ai/claude-agent-sdk-linux-x64-musl@0.3.269'
  - '@anthropic-ai/claude-agent-sdk-linux-x64@0.3.269'
  - '@anthropic-ai/claude-agent-sdk-win32-arm64@0.3.269'
  - '@anthropic-ai/claude-agent-sdk-win32-x64@0.3.269'
  - '@anthropic-ai/claude-agent-sdk@0.3.269'
  - '@openai/codex@0.154.0-darwin-arm64 || 0.154.0-darwin-x64 || 0.154.0-linux-arm64 || 0.154.0-linux-x64 || 0.154.0-win32-arm64 || 0.154.0-win32-x64 || 0.154.0'
  # Same union pattern for the r24-fixed Grok CLI release and its platform
  # payload packages.
  - '@xai-official/grok@1.0.30-darwin-arm64 || 1.0.30-darwin-x64 || 1.0.30-linux-arm64 || 1.0.30-linux-x64 || 1.0.30-win32-arm64 || 1.0.30-win32-x64 || 1.0.30'
  - '@anthropic-ai/claude-code-darwin-arm64@2.1.269'
  - '@anthropic-ai/claude-code-darwin-x64@2.1.269'
  - '@anthropic-ai/claude-code-linux-arm64-musl@2.1.269'
  - '@anthropic-ai/claude-code-linux-arm64@2.1.269'
  - '@anthropic-ai/claude-code-linux-x64-musl@2.1.269'
  - '@anthropic-ai/claude-code-linux-x64@2.1.269'
  - '@anthropic-ai/claude-code-win32-arm64@2.1.269'
  - '@anthropic-ai/claude-code-win32-x64@2.1.269'
  - '@anthropic-ai/claude-code@2.1.269'
  - '@xai-official/grok-darwin-arm64@1.0.30'
  - '@xai-official/grok-darwin-x64@1.0.30'
  - '@xai-official/grok-linux-arm64@1.0.30'
  - '@xai-official/grok-linux-x64@1.0.30'
  - '@xai-official/grok-win32-arm64@1.0.30'
  - '@xai-official/grok-win32-x64@1.0.30'

patchedDependencies:
  '@electron/osx-sign@1.3.3': patches/@electron__osx-sign@1.3.3.patch
  '@yao-pkg/pkg@6.21.0': patches/@yao-pkg__pkg@6.21.0.patch
  node-pty@1.2.0-beta.15: patches/node-pty@1.2.0-beta.15.patch
