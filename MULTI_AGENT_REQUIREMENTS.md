# 多智能体软件开发公司：产品与开发需求规格

版本：1.0。创建日期：2026-09-07。定稿整理日期：2026-09-08。

状态：按用户授权形成的开发需求基线，可继续由用户调整；不是已实现功能清单。

用户明确要求系统能够开发 App、网站和小程序，具备美工与设计岗位，使用 PostgreSQL，GBrain 也使用该数据库服务，支持组件平滑升级，并在系统层统一配置全局铁律和项目铁律。其余产品规则由本规格依据用户“别的需求都你来定”的授权确定，不再保留为一串未决选项。具体账号、密钥、发布目标、签名材料和未获授权的模型配置仍只能由用户提供或明确授权。

本文是需求基线，不是功能已实现的声明。用户随后授权按 [节点开发与硬审核规则](NODE_DEVELOPMENT_RULES.md) 开始开发：真实 Codex 以 `gpt-6-astra / max` 规划，ZCode 实施，真实 OpenCode 以 `glm-5.3 / max` 硬审核，通过后才能规划下一节点。该安排是本项目的实施流程，不自动固化为产品全部岗位的永久分工；既有隔离、配置授权及真实验证要求仍适用。

## 1. 产品目标与交付范围

### REQ-001：基于当前 dsh 建设软件开发公司

基于用户的 [dsh861 fork](https://github.com/wmqfl861/dsh861) 构建自托管的多智能体软件开发平台。用户担任公司所有者及最终业务决策者，系统组织不同岗位完成需求、产品、设计、美工、工程、测试、审核、发布准备和维护。

平台服务于多个独立软件项目。每个项目拥有需求、规则、设计、代码、环境、任务、产物和知识，支持从零开发及维护既有代码库。岗位是职责模板，成员具有持续身份，执行实例才是一次真实运行；不用每个岗位常驻一个进程。

首版面向一个所有者管理多个项目，可以邀请人类协作者并授予项目范围的查看、执行、审核或管理权限。多租户商业 SaaS、外部 agent 市场和企业计费不作为首版交付条件。

### REQ-008：App、网站与小程序交付能力

| 产品类型 | 首版交付范围 | 必须具备的产物 |
|---|---|---|
| 网站与 Web 应用 | 响应式官网、业务网站、后台管理与常见 Web 业务应用。 | 可运行前后端、设计及素材、数据库迁移、测试证据、预览环境、部署与维护说明。 |
| 移动 App | Android 与 iOS，默认采用可共享业务代码的跨平台开发方式，允许需要时增加原生模块。 | 源码、可交互原型、图标及启动资源、测试结果、构建配置、可验证安装包或真实构建记录。 |
| 小程序 | 首版以微信小程序为标准交付目标，其他平台作为后续项目适配。 | 源码、设计及切图、后端接口、开发工具预览、测试结果和上传所需材料。 |

项目模板默认采用 TypeScript：网站使用 React 体系，App 使用 React Native/Expo，小程序使用 Taro/React，后端采用 Node.js 和 PostgreSQL。具体依赖版本在项目建立时固定为经验证的兼容版本；既有项目优先遵循原有技术栈。架构岗位可以在项目约束内调整框架，但不能自行改变用户明确指定的技术或模型配置。

首批验收项目采用同一类“预约与服务管理”业务，分别交付网站、App 和微信小程序，包含列表、详情、表单校验、预约状态、用户账户及后台管理，用于验证跨岗位交付能力。它是验收样例，不是所有项目必须采用的业务或工作流。

无真实平台凭据时，接口和登录可在项目专用测试环境中模拟，但必须标注；源码、预览或模拟测试不能冒充签名安装包、真机通过或商店上线。iOS 签名构建需要受支持的 macOS/Xcode 节点或用户授权的构建服务；小程序上传需要 AppID 和对应授权。发布准备完成与外部平台审核通过分别记录。

### REQ-009：产品设计、UX/UI 与美工

设计工作包含目标用户、信息架构、用户流程、交互原型、页面结构、组件与状态规范。美工工作包含品牌视觉、配色与字体、图标、插画、Banner、启动资源、商店展示图和项目需要的其他视觉素材，两类工作分别有交付与审核责任。

关键页面交付可查看的设计稿和可交互原型，设计规范说明布局、间距、字体、颜色、组件、响应式变化，以及加载、空数据、错误、禁用和无权限等状态。美工交付可编辑源文件、适用尺寸的导出文件及素材使用说明；只有概念文字或截图不算完整设计交付。

素材必须记录来源、许可证或生成工具及必要使用限制。优先使用可授权使用的自制或项目素材；图像生成、字体购买和设计 SaaS 通过系统已授权工具执行，不因设置美工岗位就自动购买服务或调用未授权模型。缺少能力时记录阻塞，不伪造素材或使用来源不明资产。

设计审核查看实际渲染效果与核心交互；工程实现后还要进行设计还原检查。业务页面以 WCAG 2.2 AA 相关要求为默认无障碍目标，并验证键盘操作、可读对比度和适用的屏幕阅读语义。视觉审核不能只用程序检查或开发者自报替代。

## 2. 公司岗位、成员与项目空间

### REQ-005：岗位体系与分工

| 岗位 | 主要责任 | 主要交付 |
|---|---|---|
| 产品经理 | 澄清目标、业务流程、范围和验收标准。 | PRD、用户故事、验收条件、范围变更记录。 |
| 项目经理/协调者 | 制定任务计划、分工、依赖、进度与阻塞处理。 | 可执行计划、状态汇总、交接安排。 |
| 技术负责人/架构师 | 架构、接口、数据模型及技术约束。 | 架构说明、接口定义、技术决策。 |
| UX/UI 设计师 | 用户体验、交互和界面设计。 | 用户流程、原型、设计稿、组件规范。 |
| 视觉设计/美工 | 品牌、图标、插画和产品素材。 | 可编辑素材、导出资源、来源与授权记录。 |
| Web 前端工程师 | 网站和管理后台实现。 | 前端代码、页面与交互测试。 |
| App 工程师 | Android/iOS 客户端实现。 | App 代码、平台适配、构建产物。 |
| 小程序工程师 | 微信小程序实现及平台适配。 | 小程序代码、开发工具预览和上传准备。 |
| 后端/数据工程师 | API、鉴权、数据库及服务集成。 | 服务代码、接口、迁移与数据验证。 |
| 测试工程师 | 用例、自动化、回归和跨端测试。 | 测试计划、执行证据、缺陷单。 |
| 独立代码审核员 | 按项目规则复核修改及风险。 | 与具体产物版本关联的审核结论。 |
| 发布/运维工程师 | 构建、环境、部署准备及运行维护。 | 发布包、部署记录、回退和运维说明。 |
| 文档/知识管理员 | 交付文档、项目知识整理及经验归档。 | 使用手册、交接文档、经确认的知识。 |

用户保留最终范围、预算、铁律、模型配置及发布授权。协调者可以分配任务，但不能代替必要的独立审核或扩大岗位权限。多个岗位可以由同一成员兼任，但独立审核不能由被审核修改的执行实例自审；无法满足独立性时任务显示等待审核。

交付产品中的开发、修复和复核岗位由用户配置，本规格不永久绑定具体产品。本项目自身的实施人选已由用户安排为 Codex 规划、ZCode 开发和 OpenCode 硬审核，详见 [节点规则](NODE_DEVELOPMENT_RULES.md)。所有实际调用仍须遵守适用的真实身份、模型和推理设置要求，不能把本项目实施授权扩大为产品的默认岗位授权。

### REQ-010：成员身份与空间

一个岗位可以配置多个成员，同一成员可以承担多个任务；每次并发执行具有独立会话、任务目录及身份记录。成员的长期身份、规则和记忆不随 CLI 产品或某个进程终止而消失。

系统为成员保存独立配置和可变运行数据；同产品、同版本的只读程序文件可以在系统内部共享。账号或凭据可以由用户授权给多个成员使用，但连接身份、日志和读写权限仍须分别记录，不用共享账号冒充共享所有权。

代码任务默认使用项目专用分支和独立工作副本。多个任务不直接争用同一份可写目录；测试、审核默认读取指定产物版本。整合任务负责合并和解决冲突，用户原有的未提交内容不得被静默覆盖。

设计与其他文件产物同样绑定项目、任务、作者和版本。任务结束时保留交付物和记录，清理只覆盖本系统拥有且已确认无引用的临时资源。开发中的一次性试验遵守用户的临时目录规定，使用 `D:\Temp_projects` 内语义化子目录。

## 3. Harness 接入与隔离

### REQ-002：接入四种真实执行产品

首版必须接入 Codex、Claude Code、OpenCode 和用户指定的 Grok CLI。每个入口启动实际产品，不使用一个内置模型换名称冒充另一个产品。加入新 harness 通过明确的适配能力登记完成。

成员可以选择多个已获授权且满足岗位需求的执行配置；每次任务记录实际 harness、程序版本、模型设置及工具能力。新任务可选择不同 harness；运行中切换只发生在任务步骤交接点，使用可验证的产物与上下文继续，不承诺跨产品原生会话迁移。

若用户为某岗位明确指定产品或模型，切换不能绕过该指定；系统不自行给开发、复核岗位设置固定人选。普通岗位缺少必需工具、权限或配置时显式拒绝启动或报告阻塞，不静默删减能力、换模型或借用全局安装。

每个适配器至少验证启动、输入输出、真实程序身份、受控取消、系统工具连接和规则提供。续接、结构化输出、用量、细粒度原生工具观测等能力分别声明支持情况；功能不受支持时提供真实的步骤级交接方式或明确阻塞。

### REQ-004：Grok CLI 来源

Grok 接入对象为 npm 软件包 `@xai-official/grok`。用户提供的来源命令为：

```sh
npm i -g @xai-official/grok@latest
```

该命令确认产品来源，不代表实际部署写入机器现有全局环境。版本解析结果进入组件管理并固定为具体版本，不能在每次启动时不经验证追随 `latest`。

### REQ-003：与全局安装完全隔离

全部 harness 使用本系统专用的程序、配置、登录状态、凭据读取范围、skills、MCP、hooks、插件、会话、记忆、缓存和日志。安装、升级、卸载或任务执行不得读取继承或修改用户机器上的全局 agent 数据。

系统需要识别各产品的用户级、项目级及其他产品兼容配置发现路径；不因某文件能被原生 CLI 自动发现，就加载未获授权的扩展或规则。项目原有规则可以经过系统导入检查后纳入项目配置，原文件不被静默重写。

不能阻止未经授权配置读取或原生工具越权的执行环境，不得用于要求相应强制限制的岗位。原生 Shell、文件读写和网络访问的实际限制必须覆盖系统工具以外的路径，不能只靠提示词声明隔离。

## 4. 系统级铁律与操作授权

### REQ-011：全局铁律和项目铁律统一管理

控制台提供全局铁律和项目铁律管理，作为所有岗位及 harness 的统一配置入口。用户维护一次规则，系统负责在各执行环境中提供和执行；不要求用户逐个修改 `AGENTS.md`、`CLAUDE.md`、CLI settings 或 MCP 配置。

每条规则保存唯一标识、作用范围、规则类型、内容、负责人、版本、生效条件和变更记录。支持两种相互配合的表达：可执行的结构化限制，以及需要模型理解、审查或人工验收的行为规范。自然语言生成结构化规则时必须显示结果，无法转换的要求保持明确的审查义务，不宣称已获得程序强制保证。

规则合成顺序为公司全局基础、项目约束、岗位约束、单任务授权。权限按允许范围取交集，显式禁止优先；项目只能增加或收紧全局铁律，不能覆盖削弱。需要例外时，由所有者修改或签发允许例外的全局规则，记录对象、范围和期限。规则冲突在执行前报告，不由 agent 自行猜测取舍。

只有所有者能发布全局铁律及全局例外。项目管理员能在授权范围内发布项目规则；agent 可以提出变更建议，但不能修改自己的治理要求。外部网页、工具结果、记忆、项目文本或其他 agent 的消息不自动成为更高优先级的规则或操作授权。

系统将本次适用规范提供给 harness，必要时生成仅属于本系统的原生格式文件或启动参数。生成物是配置投影，系统中的版本化规则才是管理来源；不得以“自动分发”作为允许修改全局安装的理由。硬性规则不可因上下文压缩或长度限制被静默删去，无法完整提供所需规则时不得开始受其约束的工作。

可执行限制在调度、文件/网络/工具访问、凭据使用、预算及发布入口执行；检查或文字要求通过测试、审查及验收核实。限制无法在所选 harness 上落实时，系统显示不兼容，不以“已注入提示词”冒充强制执行。

普通规范随下一步骤重新组合并记录版本。权限撤销和新增禁止对新操作立即生效；已发出的外部操作不能保证撤回，系统尽力中止自有可取消工作并记录结果。扩大的权限不自动追加到正在运行任务，需要适用的新授权。运行记录保存初始规则版本及后续限制变更。

初始全局规则包含用户已有的真实 agent 身份要求、指定独立审核、模型配置变更授权、全局安装隔离、系统资源所有权以及真实结果报告要求；完整规则可在系统中维护，不在各 harness 私自复制为另一套可漂移标准。

### REQ-012：操作授权与人工决定

任务在创建时确定修改范围、可用环境、预算和交付目标。在该范围内的读取、开发、测试及预览可以自主执行，有效授权持续适用，不对每个相同范围操作重复询问。

生产发布、商店提交、删除或覆盖用户数据、不可逆数据库迁移、对外发送内容及新增费用承担，需要具体目标和内容的授权；已存在完全匹配且仍有效的持续授权时可复用。用户批准对象变化或规则收紧使授权失效时，再提出具体决定请求。

批准记录关联操作、对象、产物版本和条件。执行者不能自行批准自己的受限操作；无人响应时保持等待状态，不把沉默或超时视为同意。系统级铁律变更、模型/提供商/endpoint/推理等级/Key 等变更仍遵循用户的具体授权要求。

## 5. Skills、工具、美术能力与凭据

### REQ-015：系统集中提供岗位能力

系统统一安装或接入业务 tools、skills、外部 MCP 及兼容扩展，按岗位、项目和执行实例授权。harness 连接系统能力入口，由系统建立真实身份及任务范围；模型自报岗位名称不是授权依据。用户无需逐项到每个 harness 安装业务扩展。

Skills 在系统中维护正式内容、版本、资源和依赖。系统提供获准目录和按需读取，模型取得说明后使用；需要执行的脚本或业务动作由获准工具完成。原生 slash 命令、自动触发或 hooks 的体验需要相应适配，不假定远程读取 skill 就获得这些行为。

系统工具包括文件与代码操作、测试构建、浏览器与预览、图形和素材处理、接口与数据库验证、文档交付及平台发布连接。每项工具明确执行环境、参数、文件位置、数据权限、返回结果及取消能力；美工所需图像能力必须真实接入已授权服务或本地工具。

每个扩展记录来源、版本、依赖、支持的 harness、许可及验证状态。产品专属 UI 或生命周期插件只有可独立执行的部分可作为通用系统工具，其余按兼容性支持，不静默伪装成通用能力。

工具列表过滤和实际调用权限保持一致；直接猜测工具名称也不能越权。凭据由系统的凭据管理持有，以必要范围提供给执行进程或服务；普通 agent 不获得平台/GBrain 数据库账号，不把密钥写入对话、记忆、素材或普通日志。

扩展编辑由获准管理员发布版本；agent 可提交改进建议，但不直接修改生效 skills、铁律、模型或工具权限。运行中的任务继续使用已固定的扩展版本，权限撤销除外。组件升级规则见 REQ-020。

## 6. PostgreSQL、GBrain 与数据归属

### REQ-016：统一 PostgreSQL 数据库服务

PostgreSQL 是系统的主数据库服务，GBrain 也使用本系统管理的 PostgreSQL 后端，配置其兼容的 pgvector 扩展。首版不以 PGLite 或 GBrain 自带任务队列替代平台主数据库与工作流运行管理。

“共用数据库”落实为共用一个受管理的 PostgreSQL 服务，平台与 GBrain 默认使用独立逻辑数据库、迁移记录和数据库账号。这样可以统一部署、备份与监控，同时避免 GBrain 自身升级直接修改平台表。平台不直接读写 GBrain 私有表，GBrain 不访问平台控制表。

| 数据 | 归属和访问方式 |
|---|---|
| 公司、项目、岗位、成员、规则、授权、配置版本 | 平台 PostgreSQL，系统服务管理。 |
| 计划、任务、依赖、状态、审批、运行检查点、派发与重试记录 | 平台 PostgreSQL，作为工作流控制事实。 |
| 组件版本、升级记录、用量及产物索引 | 平台 PostgreSQL，关联真实运行证据。 |
| 长期知识、实体、事实及检索索引 | GBrain 管理的 PostgreSQL 数据库，通过其服务接口访问。 |
| 代码、设计源文件、图片、安装包与大文件 | 项目 Git 或文件/对象存储，PostgreSQL 保存引用、版本和校验信息。 |
| dsh 原生 Session 记录 | 遵循既有持久化格式和兼容规则，平台保存关联及所需投影。 |
| 凭据 | 系统凭据管理；数据库仅保存必要引用或受保护的密文，解密材料独立管理。 |

已发布的 dsh Session JSONL 和现有存储遵循 [仓库迁移规则](AGENTS.md)及[会话记录说明](docs/architecture.md#session-log)，不得为统一数据库而移动、覆盖或删除已提交记录。新增平台结构化业务状态由 PostgreSQL 承担；原生记录的迁移只有经过显式适配和验收才发生。

交付项目的开发、测试、预览和生产数据与平台/GBrain 隔离。开发环境可以共用 PostgreSQL 服务但使用独立项目数据库和账号；正式交付环境使用项目自己的数据库配置，不暴露控制平台或记忆库。

任务状态、授权及派发意图必须先持久化再确认接受，避免服务重启后忘记已接单工作。组件之间通过带任务标识的事件或接口同步，不能把同一状态维护成两个互相矛盾的权威副本。数据库不可用时不确认未落盘的新任务或受控操作。

### REQ-007：GBrain 共享长期记忆

选定 [garrytan/gbrain](https://github.com/garrytan/gbrain) 作为长期记忆组件，使用 REQ-016 的 PostgreSQL 服务。采用已验证的固定版本，版本选择不改变此前 [GBrain 适配评估](GBRAIN_MEMORY_ASSESSMENT.md) 中尚需集成验证的事实。

记忆分为公司共享知识、项目知识、岗位方法和成员个人记录。公司知识由指定管理者发布，项目知识只对该项目授权成员开放；成员个人空间以独立数据 source 和身份授权区分，不用同一 source 下的文件夹冒充读取隔离。GBrain 的 `private` 可见性不直接等于跨 harness 的成员私有记忆。

系统根据任务和身份读取必要的上下文，在新任务、步骤交接及必要的上下文刷新时提供给所选 harness；有用结论在任务交接或完成时写回。记忆连续性由稳定身份、授权和实际读写保证，不依赖每个产品恰好存在同样的原生 hook。

默认将用户明确确认的事实、已验收项目结论和带证据的执行观察写为可追溯记录；agent 推测和未复核经验保留为待验证条目，不直接替换正式知识。共享结论的更正保留前后关系、来源与时间，冲突进入责任岗位处理，不静默覆盖。

不自动导入机器全局会话或个人邮箱等外部资料。涉及第三方资料、敏感信息、遗忘和导出时按授权执行。记忆事实失效、删除原文和清除备份分别记录，不能承诺一项 forget 操作删除所有副本。

GBrain 不作为流程状态、审批或铁律的唯一来源。记忆服务暂不可用时，仅允许不依赖它的步骤继续；必需记忆步骤等待恢复，并显示限制。模型与记忆额外调用的 provider、embedding、reranker 或综合模型采用用户已批准配置，未获授权时不得自行填入供应商、模型或 Key。

## 7. 工作流、任务、交付与恢复

### REQ-006：用户可规划和调整工作流

提供自然语言规划、结构化任务编辑以及可查看和编辑依赖的流程图。三种入口修改同一份计划，不维护互相漂移的脚本副本。用户可以保存模板、复制修改、比较版本，并针对不同业务生成不同流程。

每步配置目标、岗位、输入、产物、依赖、执行配置、工具集合、验收规则、预算与失败处理。支持串行、并行、条件分支、聚合、交接、评估后返工、人工决定和等待事件。重复逻辑可保存为子流程；循环必须有退出条件与上限。

默认提供新项目、已有项目迭代、缺陷修复、设计改版、测试与发布准备模板。模板可按项目裁剪和调整，只保留适用的验收和治理义务；不强制所有任务按同一顺序经过所有岗位。

协调者可在已批准范围内细分任务、安排依赖和重排未开始步骤，记录变更原因；新增目标、扩大预算或权限、改变模型及取消质量要求需遵循授权。已运行步骤的计划调整以明确的暂停/交接操作处理，不能直接修改历史。

首版内部协作使用统一的任务与消息模型，各 harness 通过已支持的接口接入。A2A 作为以后对接独立服务的可扩展入口，首版不依赖它完成本地协作；ACP、SDK、CLI 和 MCP 各自的语义保持明确，不互相冒充。

### REQ-013：任务、产物与验收状态

任务状态区分草稿、可执行、排队、运行、等待输入、等待批准、待审核、返工、验收通过、失败、取消及状态待核实。执行结束不是自动验收通过，返回码零或一句“完成”不足以使任务通过。

每份产物带有项目、任务、作者、生成配置、版本、文件或对象引用及必要校验信息。设计、代码、构建、测试、审核和发布材料形成可查询关联；变更导致已有审核失效时明确标记，需要对相关变化重新审核。

消息包含发送者身份、目标任务、关联运行、消息类型、正文及产物引用。收到消息、任务执行和验收是独立事实；避免将自由聊天内容作为唯一状态。按需交接上下文，不向所有成员广播完整会话或私有记忆。

### REQ-014：质量、独立审核与发布

按项目类型执行单元、接口、集成、端到端及必要平台测试，验证功能、错误状态、鉴权、数据迁移和实际构建。用户可见变更包含真实预览、视觉和交互检查；App 和小程序还包含对应平台预览及适用真机验证。

代码审核由用户安排的独立审核者执行，遵循 REQ-005 的身份与授权要求；设计审核由非产出实例或用户查看渲染结果；测试证据指向执行命令、环境与产物，不以空测试、模拟截图或描述替代真实结果。测试及审查各有观测限制时必须明示。

初始发布标准：阻断问题和高严重度问题为零；关键业务验收用例全部通过；必要的独立复核、设计检查、构建和凭据检查完成。例外只能由有权人员对具体问题和版本批准，不能由自动投票覆盖。

交付包包括源码、设计及素材、构建或预览、迁移与接口说明、测试审核证据、部署及维护文档和已知限制。部署到已授权的预览环境可以自动执行；生产发布、App 商店和小程序正式提交按具体发布授权执行。

### REQ-018：持久执行、失败处理与取消

工作流的任务状态、输入版本、已完成产物、等待条件和执行尝试写入 PostgreSQL。后台执行不依赖浏览器保持打开；服务重启后重新识别尚在运行的 worker 与需要恢复的步骤。

数据库事务只保证控制记录一致，不能保证外部工具天然只执行一次。为可重试操作保存稳定操作标识及结果凭证；回传失败时先核实是否已发生外部动作，无法确认则保持状态待核实，禁止直接重复发布、扣费或其他不可逆动作。

默认只对确认未发生副作用的暂时性错误自动重试最多两次；限流按服务等待信息退让，超过预算或时间限制时停派。连续两轮没有新证据或有效产物时报告无进展，不无限循环。验收返工默认最多三轮，超过后进入协调者或用户决定。

用户可暂停后续派发、停止单个任务及其子树，或停止全系统接收新工作。停止请求与确认停止分别呈现；仅管理本系统拥有的进程和资源。已完成的文件或外部操作不会因为取消而被假装撤销。

跨 harness 恢复以步骤产物和明确交接为基础；不支持原生续接时新建实例，并标记为交接或重新执行。系统不得把模型缓存、隐藏推理或原生 session id 迁移作为无条件能力。

## 8. 控制台、真实记录与资源管理

### REQ-019：统一公司控制台

控制台使用中文作为默认界面，提供公司概览、项目中心、岗位与成员、流程编辑、任务看板、设计和产物预览、待批准/待审核事项、skills 与工具、GBrain 知识访问、组件升级、铁律管理、数据库健康和用量查看。

用户能从任务看到当前执行者、harness、版本、工作内容、等待原因、输出和下一责任人。支持对任务补充要求、限定范围地调整计划、查看有效规则、批准具体操作、暂停取消和打开产物。

初始通知使用系统内消息和浏览器内状态；外部邮件、聊天软件或手机通知需单独授权连接。关闭页面不会结束服务端任务，重新进入后能恢复查看。复杂外部通知、多租户收费及公共市场不列入首版。

### REQ-017：运行真实性与审计

记录真实程序路径及校验信息、组件版本、成员/岗位/任务、初始执行配置、适用规则、授权、系统工具请求与结果、产物、失败及升级事件。模型信息区分配置声明与运行证据；不可取得的用量或内部行为显示未知，不补写虚构细节。

系统提供和接收的模型可见内容保持可追溯，并关联 dsh 原生会话记录。只返回最终答案的外部适配器不能被展示为已取得完整工具过程；不索取或伪造隐藏推理。审核身份、执行身份与普通子 agent 名称明确区分。

控制状态和授权事实必须可靠保存；普通诊断日志可以按策略轮转，关键审计不能因清理普通日志丢失。记录包含可还原的引用及访问权限，敏感值脱敏；用户能导出项目交付和审计材料。

### REQ-021：默认预算与执行上限

下列为首版默认值，由系统设置或更严格项目规则调整；不是本轮实际消费授权。数值属于可配置部署参数，不能硬编码进插件。

| 参数 | 初始值 | 达到限制时 |
|---|---|---|
| 全系统同时运行的真实 agent 实例 | 4 | 后续任务排队。 |
| 单项目同时运行的真实 agent 实例 | 2 | 不影响其他项目的已获配资源。 |
| 可安全自动重试次数 | 2 | 保留失败及已有产物，等待处理。 |
| 验收返工轮数 | 3 | 提交问题和证据，等待重新决定。 |
| 单步骤连续运行时长 | 60 分钟 | 停止新增动作，尝试受控中断并保留状态。 |
| 单次工作流持续时长 | 24 小时 | 等待明确延期，不无限执行。 |
| 单任务可计量费用上限 | 20 美元 | 暂停新的收费调用。 |
| 系统滚动 24 小时可计量费用上限 | 100 美元 | 排队并显示预算原因。 |

达到费用上限的 80% 发出系统内提示；达到上限不再启动新的收费操作。已发出的调用和延迟账单可能超出估算值，系统显示实际差异，不能承诺供应商侧绝对封顶。无法估价的收费服务必须具有额外调用次数或服务侧额度限制后才能自动使用，未知成本不记零。

费用统计包括 harness 推理、重试、子 agent、GBrain embedding/重排/综合、图像服务、构建及第三方工具等能取得的数据。订阅用量、预估金额和实际计费分别显示。额度不足不授权系统擅自换模型、Key、provider 或全局账号。

## 9. 组件平滑升级

### REQ-020：流程连续、版本可控的升级

升级目标是用户不必停止公司、重建流程、重新配置岗位或反复登录。兼容的 CLI、适配器、工具、skills 与无状态服务升级不得中断已接受的工作、丢失产物或重复外部操作；升级行为可在组件中心查看，不把“无感”解释为对用户隐瞒版本变化。

组件管理记录真实来源、固定版本、兼容能力、依赖、配置与数据版本以及验证结果。程序版本与模型配置分别管理；CLI 升级不得改变已批准模型、思考等级、provider、endpoint 或凭据读取范围。上游新默认影响这些项目时，升级不能自动成为生效版本。

升级按检查、预备、验证、有限范围试用、切换新流量、旧版本完成工作和保留恢复版本的顺序进行。默认允许后台检查和预备；兼容版本在通过规定验证、满足系统已授权升级策略后自动接收新运行。破坏性数据迁移或权限/模型变化需要具体授权，不由一般自动升级策略涵盖。

每次工作流开始时固定所需 harness、适配器、tools、skill 内容和执行配置版本。升级期间已运行流程及其尚未开始步骤继续使用原依赖，新的流程使用通过验证的新版本；不在一个活跃 CLI 进程内部替换程序文件。确需迁移长任务时，在经过验证的步骤交接点建立新执行版本并记录变化。

无状态工具服务的新旧版本可以并存；有状态会话需要在其支持的结束或交接点切换。长连接、原生会话和后台任务不能直接被新程序接管时继续保留旧版本，不强制杀死来宣称升级完成。旧版本只在无运行引用后清理，默认保留最近三个已验证版本且不少于七天，仍被任务引用的版本不受该清理期限影响。

数据库和 GBrain 升级须验证数据格式、新旧读写兼容和索引变化。可兼容迁移先扩展、验证并切换，移除旧字段或改变不可兼容语义只在没有旧消费者且有恢复准备后实施。旧程序无法读取新数据时不允许简单回退；恢复备份可能丢失切换后的写入，必须明确数据影响并取得所需授权。

新版本验证或试用失败时，不更新默认路由；已切换后的异常对未开始的新工作路由回兼容版本，保留问题记录，禁止用另一模型或全局安装替代失败版本。紧急权限撤销或已确认不应继续运行的版本可以触发受控停用，规则执行优先于不中断承诺。

普通兼容 CLI/工具升级的验收目标为：已接受任务丢失数为零、升级引起的现有任务中断数为零、重复外部副作用为零、用户重建流程次数为零。通过本系统故障探测后，应在 60 秒内完成新任务路由回退；这不是外部模型响应时间承诺。

PostgreSQL 主版本升级、主机重启、单实例数据库维护、外部接口撤销和不可逆 schema 变更不能无条件保证零停顿。具备经验证冗余与切换能力的部署可执行在线切换；否则使用明确的维护窗口，暂停新步骤，保留任务和产物并恢复。不得把维护暂停标为零中断，也不得承诺任意旧 dsh Session 格式支持降级。

## 10. 部署、备份与运行目标

### REQ-022：部署与构建环境

采用自托管服务和可登记的执行节点。默认受支持的服务部署模板为 Linux 节点运行平台服务、PostgreSQL 和 GBrain；当前 Windows 主机作为浏览器操作端与本地 CLI/项目执行节点。需要 iOS 时登记 macOS 构建节点或已授权服务，Android 和微信小程序使用经过验证的对应工具链。

平台必须展示节点的操作系统、工具链和能力，按任务选择符合要求的节点。当前 Windows 环境不自动假定支持 Docker/WSL、GBrain 全部原生插件或 iOS 构建；未登记所需环境时只暂停相关步骤，其他项目继续。节点采购、远程账号和云服务开通不包含在文档授权中。

首版验收使用至少 8 vCPU、16 GB 内存和 SSD 的服务节点，执行节点另行满足各产品与构建工具要求。目标支持两个活跃项目、四个并行真实 agent；100,000 条任务/事件记录和10,000份知识文档作为控制台与检索集成的参考数据集，GBrain 模型或检索性能另行实测。

在上述参考条件和局域网下，普通元数据页面请求 p95 不高于 2 秒，状态事件从系统接收到界面更新 p95 不高于 3 秒；不包含模型推理、外部网络、检索综合、大文件下载及构建耗时。这些是待测试的验收目标，不是现有性能声明。

### REQ-023：备份、保留与恢复

平台与 GBrain 的逻辑数据库、必要文件、Git/对象存储引用和配置版本纳入一致的备份清单，密钥材料按独立受保护流程保管。默认每日完整备份，数据库持续归档，最近七日备份可恢复，关键里程碑交付保留至项目归档规则允许删除。

正常持久化确认过的任务与授权，在服务进程重启时不得丢失；整台主机或存储损毁的参考恢复目标为 RPO 不超过 15 分钟、RTO 不超过 4 小时，需在目标环境实际演练。外部平台已发生的操作、未回传结果和第三方数据损失不被假装包含在数据库恢复保证内。

默认诊断日志保留 30 天，关键任务、规则、授权、产物索引和升级审计保留至少一年；关闭项目可归档或由所有者调整保留策略。实际产物与记忆的删除有独立授权、依赖检查和记录，不以清理缓存代替业务删除。

组件和数据库变更前执行兼容验证；恢复演练确认数据、权限、引用和已接受任务一致。数据库密码、签名材料和项目生产凭据不提供给普通开发成员或记忆模型。

## 11. 验收标准

以下每项都要求实际结果及证据，不能只以配置存在、工具列出或 agent 自报通过验收。模型可能存在非确定性，系统应保存复现输入和版本，不承诺逐字相同输出。

| 编号 | 验收场景 | 通过条件 |
|---|---|---|
| AC-01 | 四种 harness 接入 | 分别有真实程序启动、任务执行和结果证据，Grok 来源正确，无名称冒充。 |
| AC-02 | 全局隔离 | 在全局环境放置可识别测试配置，系统运行不加载也不改写；反向变更亦不影响专用环境。 |
| AC-03 | 配置和模型真实性 | 选择与实际启动配置对应，未知字段标注；缺配置不降级或借全局程序。 |
| AC-04 | 同成员换 harness | 身份、岗位授权和获准记忆连续；原生会话迁移不被虚构。 |
| AC-05 | 岗位空间并发 | 两成员并行产物不覆盖，冲突显式呈现，用户既有修改保留。 |
| AC-06 | 全局/项目铁律 | 一次系统配置适用于四种 harness；项目不能削弱全局，规则来源和版本可查。 |
| AC-07 | 权限实际执行 | 未授权工具、目录、网络或数据访问被实际拒绝，原生工具不能绕过。 |
| AC-08 | 铁律冲突和撤销 | 冲突在执行前报告；撤销后新操作被阻止，已发出操作状态准确。 |
| AC-09 | 授权复用 | 范围内开发不重复请示；变更发布目标或产物后原批准不被错误沿用。 |
| AC-10 | Skills 与工具集中提供 | 用户只在系统管理资源和岗位分配，四种 harness 获得正确集合，未知依赖明确失败。 |
| AC-11 | UX/UI 和美工交付 | 有可交互原型、设计规范、可编辑素材和来源记录，实际渲染检查通过。 |
| AC-12 | 网站交付 | 参考业务可运行、响应式和关键状态正确，独立审核与测试证据完整。 |
| AC-13 | Android 交付 | 实际构建安装包并在目标环境验证关键业务，不以网页预览替代。 |
| AC-14 | iOS 交付 | 经授权 macOS/构建服务得到真实构建与适用测试结果；签名或账号缺失不得显示已上架。 |
| AC-15 | 微信小程序交付 | 开发工具预览和关键业务可用，适用真机与上传验证有证据，AppID 缺失明确显示。 |
| AC-16 | PostgreSQL 数据隔离 | 平台、GBrain、交付项目逻辑库和账号权限隔离，正常账号不能访问其他域。 |
| AC-17 | GBrain 跨 harness 记忆 | 同一成员在不同产品记住、读取、更正同一事实，并保留来源和授权。 |
| AC-18 | 共享与私有知识 | 共享知识按许可可读，项目及成员私有数据在搜索和直接读取中均不能越权。 |
| AC-19 | 流程编辑和模板 | 通过自然语言及编辑界面形成计划，支持分支、并行、返工和人工节点；模板与运行版本分别保存。 |
| AC-20 | 独立审核与版本 | 开发和复核实际执行者符合用户安排，独立审核绑定具体版本，改动后的审核状态正确。 |
| AC-21 | 崩溃与交接 | 进程重启后已确认任务和产物可找回，无法原生续接时明确新实例交接。 |
| AC-22 | 外部副作用重试 | 操作完成但回传失败时不重复执行，不能确认的状态保持待核实。 |
| AC-23 | 取消与资源所有权 | 停止任务及其自有子树，不干扰其他任务、全局 CLI 或用户进程。 |
| AC-24 | 预算和限流 | 达上限停止新收费调用，费用估计/未知正确呈现，不擅自换模型。 |
| AC-25 | Codex 运行中升级 | 旧流程及其后续步骤继续用旧版本，新流程可用经验证新版本，工作不中断。 |
| AC-26 | 工具和 skill 升级 | 运行固定版本不漂移，新版本验证失败不进入默认路由，无重复副作用。 |
| AC-27 | 组件回退 | 模拟新版本故障，60 秒内回退新任务路由到兼容版本，产物和记录不丢失。 |
| AC-28 | 数据迁移不兼容 | 无读写兼容或恢复条件时拒绝无感切换，维护影响明确；不破坏已发布 Session 记录。 |
| AC-29 | 记忆或数据库失联 | 不确认未持久化操作，依赖步骤等待，独立可执行工作按限制继续，恢复状态正确。 |
| AC-30 | 备份恢复 | 在独立环境恢复数据库和产物引用，验证权限及任务一致性，达到目标 RPO/RTO。 |
| AC-31 | 控制台和真实性 | 用户可查看状态、规则、版本、产物及待决定事项，缺失原生轨迹不被伪造。 |
| AC-32 | 性能与代表性任务集 | 在参考环境记录性能、交付通过率、费用和人工介入，外部耗时与平台耗时分别统计。 |

## 12. 开发组织与里程碑

具体开发顺序、工作包依赖和验收条件见 [开发路线与工作包](MULTI_AGENT_DEVELOPMENT_ROADMAP.md)，当前实施遵循用户指定的 [节点规则](NODE_DEVELOPMENT_RULES.md)。阶段路线不是逐文件实施计划；真实 Codex 逐节点编写计划，ZCode 实施后经真实 OpenCode 硬审核，通过才继续。实际节点状态以 [执行状态](development/NODE_STATUS.md) 和审核回执为准。

M1 建立可验证的系统基础：四种真实 harness 的隔离适配、PostgreSQL、GBrain 接入、成员岗位、系统铁律、资源入口、基本任务持久化与实际权限拒绝。涉及模型配置缺失或产品不可用时保留明确阻塞，不用替身通过。

M2 形成软件交付过程：需求、设计、美工、工程、测试、独立审核和发布准备，完成参考网站、Android/iOS App 与微信小程序的验证，并提供计划编辑、模板、控制台和产物关联。

M3 完成长期运行验收：持久恢复、预算、并发冲突、组件并行版本升级、回退、数据库兼容检查、备份恢复与权限变更验证。只有通过适用 AC 项目才称为完整首版；中间里程碑不假称所有能力已交付。

实施优先利用 dsh 的文档化扩展点，遵循 [架构](docs/architecture.md)、[测试规则](docs/testing.md)和 [Agent Note 规则](.agents/notes/README.md)。实现与复核分别由用户安排的执行者承担；本需求文档不指定未经核实的 package 修改路径，不替用户分派 agent，也不授权通过冒名调用替代独立审核。

首版不包含自动修改铁律或模型的自进化、跨组织 agent 市场、复杂企业计费，以及承诺任意不兼容升级都零停顿。后续加入 A2A、更多小程序平台或原生专属能力时，沿用同一身份、权限、版本与验收规则。

## 13. 外部输入与事实限制

产品规则已按授权确定。执行具体项目仍需用户给出该项目业务目标、品牌或素材要求、仓库和发布对象，以及需要的账号、AppID、签名证书和明确批准的模型配置；系统通过项目表单和范围明确的授权收集，不在本规格中捏造。

当前只有需求和调研文档，尚未完成适配、数据库安装或产品验收。已知 dsh 外部 Codex/Claude 适配为一次性任务，现有工作流没有完整的模板和检查点恢复；中央铁律、能力服务、PostgreSQL 平台状态及平滑升级均需实现与验证。现有证据见 [工作流调研](MULTI_AGENT_WORKFLOW_RESEARCH.md)与 [GBrain 评估](GBRAIN_MEMORY_ASSESSMENT.md)。

原 [需求补充清单](MULTI_AGENT_REQUIREMENTS_SUPPLEMENT.md)保留为覆盖追踪，相关决定以本规格为准。此前的 Q-003 至 Q-017 由本文范围、岗位、空间、规则、记忆、工作流、部署及验收要求承接，不再作为等待用户逐项回答的阻塞项。

## 14. 需求来源与授权

2026-09-07，用户要求持续收集需求并允许对不明确之处提问。主要原话如下，保留用于追踪，不作为与本文现行规则并列的另一份规格。

> 首先，我的需求是，要把各个常用的agent纳入进来，比如codex，claude code，opencode，grok cli这几个。然后。这个几个是单独给这个系统安装的，和全局安装的分开。

> agent全部隔离，和系统安装的完全隔离，grok cli是这个安装命令：npm i -g @xai-official/grok@latest

> 我会有很多不通职能的agent，我是每个agent都预先设好，用codex还是claude code决定好？空间设好，单独配置设好？还是有更好的选择？

> 如果我一个职责的agent，可以换不同的harness么？比如codex和Claude code，grok cli，可以随意选择么？记忆是否不变？另外skill和工具怎么管理？

> 所以skill和工具，比如插件和mcp只需要装一遍，不需要每个harness都装一边是吧。我理解是每个工作的岗位，都设好指定能调用的skill和工具，选择了哪个harness，就导入到哪个harness，每个agent无论使用哪个harness，都只会使用纯净harness自带的工具和skill和岗位指定的工具和skill，对么？

> 能否改skill和工具改造成装在这个系统上，然后各个harness通过系统使用skill和工具的功能？不用直接配置和安装？

2026-09-08，用户进一步说明工作流、记忆与软件公司定位：

> 接下来就是不同agent工作的工作流如何定？我现在不是要定固定的工作流，而是我需要知道之后我怎么方便的规划。现在比较先进的多agent交互是怎么做的？是loop harness么？用a2a沟通么？

> 共享记忆的记忆系统就用gbrain你觉得可行么？是不是能完全满足我的需求？

> 还有什么我还缺的？你帮我补充一下，对于这个多agent系统？

> 你帮我按照软件开发公司写需求，需要能开发app，网站和小程序。需要有美工和设计。别的需求都你来定。另外把postgre也加入这个系统中，作为数据库，gbrain的记忆系统也用这个数据库。还有一点，我希望有个功能，如果其中一个部件升级，比如codex，或者某个工具，能够不影响流程运作，无感丝滑升级。另外关于全局铁律和项目铁律，也是在系统层面就能配置好，不用一个一个harness去配置。

用户随后明确开发与复核人选由其安排，并要求如实说明阶段计划的完成程度：

> 指定哪个agent开发，哪个agent符合我另有安排，你做好你的任务就好

> 每个阶段都已经有具体计划了么？每个阶段开发还要写具体计划么？
